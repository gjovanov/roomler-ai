# Functional Requirements

Every substantial feature or multi-step program gets an **FR**: a spec in this directory
and a tracking issue in [`gjovanov/roomler-ai/issues`](https://github.com/gjovanov/roomler-ai/issues),
modeled on [`gjovanov/lgr#21`](https://github.com/gjovanov/lgr/issues/21). The convention
itself lives in `CLAUDE.md` (`## Functional Requirements (FR) workflow`) so that it loads
in every session; **this file is the number registry.**

## Claiming a number

Add your row to the registry below **in the same commit as your spec file**, then push.

That is the whole protocol — and the reason it is one shared table rather than "scan
`docs/fr/` for the highest N" is that scanning **cannot** work, with or without a
post-create re-check. Two sessions both read `max = N`, both write `FR-N+1`, and git merges
them without a murmur because they touched *different* files. Not hypothetical: the
convention collided **five times on its first day**, the closest pair seven seconds apart —
and the last three happened *after* a scan-and-re-verify rule was already in force — the
fifth even after this ledger existed (see the row-is-the-claim warning below).

| collided | resolution |
|---|---|
| `FR-1` (#767, 09:55:42) vs `FR-01` (#768, 09:57:20) | #768 → FR-9 |
| `FR-3` (#773, 10:04:58) vs `FR-3` (#774, 10:05:05) | → FR-6 / FR-5 |
| `FR-5` — three sessions within minutes | → FR-4 / FR-5 / FR-6 |
| `FR-7` (#778, 10:07:14) vs `FR-7` (#780, 10:09:41) | #780 → FR-8 |
| `FR-10` (#783, 11:45:34) vs `FR-10` (#784, 11:47:18) | #784 → FR-11 |
| `FR-24` (#838, licensing) vs `FR-24` (#840, viewer) | #840 → FR-26 |

The tie-break when one slips through anyway is **deterministic, and it is not "the younger
one"**: the **LOWER issue number keeps `FR-N`; the HIGHER renumbers** to the next free N —
title, spec filename, ledger row and in-body references together. Never renumber *into* a
vacated number, and numbers already settled stay settled. That is repair, though — not
allocation.

⚠️ The retracted "younger claim renumbers (ambiguous ⇒ the retrospective one)" rule is why
the `FR-3` collision needed a *third* commit (`7ad2ca0b`): both sessions applied it, both
concluded they were the younger, and they renumbered **past each other** into a shared
`FR-5`. Timestamps seconds apart are not an ordering, and "the retrospective one" was true
of both at different moments. Issue ids are server-assigned and monotonic, so two sessions
that never talk compute the same winner (`3af6761d`, #779).

Editing **one** file makes git the arbiter instead: the second push is rejected as
non-fast-forward, and the rebase shows the number is already taken *before* anything is
published. Same shape as the overlay block allocator, where a unique index arbitrates
concurrent slot claims rather than a lock.

⚠️ Claim **immediately before** you write, never at the start of a long session — the same
discipline as the release-tag race.

⚠️ A number is never reused, including by a withdrawn or superseded FR. Mark the row
instead: a reader who meets `FR-4` in an old commit message must land on the right document.

⚠️ **A row that is still in an open PR is not a claim either — only master arbitrates.**
The sixth collision (`FR-24`, 2026-08-28) had *both* sessions do the protocol right: each
wrote its row in the same commit as its spec. But #838's row sat on an unmerged branch
(#844) while #840's merged, so at no point did a push get rejected — git arbitrates at
merge, and one side had not reached it yet. A number is therefore only yours once the row
is **on master**; park the claiming commit in a long-lived PR and someone can take it from
under you, with the repair falling due later and costing more (here: a rename across a
shipped spec, a ledger row, an issue title and ten code comments). Land the claim first,
then take your time with the implementation.

⚠️ **The row IS the claim — a spec file alone is not.** `FR-10`'s spec reached master on
2026-08-27 (`2366859d`) without a row here, so this table still read `max = FR-9` and the
next session claimed `FR-10` 104 s later (#783 vs #784) — the fifth collision, and the first
one the registry existed to prevent and didn't. Pushing the spec without the row re-opens
exactly the hole the row closes.

## Registry

<!-- RETIRED-NAME-ANCHOR(22): the FR-21 row names what FR-21 retired; an FR title
     has to say what the FR is about. Scoped to that row rather than the whole
     table: a region ending after the LAST row put its END marker on the file's
     final line, so every appended FR row conflicted with it — for every session,
     not just this one. FR-21 sits at a fixed offset from the header because new
     numbers are only ever appended below it. -->
| FR | Issue | Title | Status |
|---|---|---|---|
| [FR-1](FR-1-remote-desktop-drag-smoothness.md) | [#767](https://github.com/gjovanov/roomler-ai/issues/767) | RustDesk-parity remote-desktop drag smoothness | in progress — P1–P5 shipped + field-verified (0.4.4 "Rozalina works nicely"), P6 + P7-HUD open |
| [FR-2](FR-2-pin-update-downgrade-guard.md) | [#770](https://github.com/gjovanov/roomler-ai/issues/770) | Pin-update downgrade guard | closed — shipped (#771) + field-verified on prod 2026-08-27 |
| ~~FR-3~~ | — | *vacated* — the #773/#774 collision; both claimants renumbered | never reuse |
| [FR-4](FR-4-conference-media-path-integrity.md) | [#776](https://github.com/gjovanov/roomler-ai/issues/776) | Conference media-path integrity | closed — shipped + field-verified (retrospective) |
| [FR-5](FR-5-macos-unattended-update-chain.md) | [#774](https://github.com/gjovanov/roomler-ai/issues/774) | macOS unattended update chain | closed — field-closed at agent rc.482 (retrospective) |
| [FR-6](FR-6-ci-release-build-speed-slo.md) | [#773](https://github.com/gjovanov/roomler-ai/issues/773) | CI + release build-speed SLO — every lane ≤10 min warm | shipped + field-verified (retrospective) |
| [FR-7](FR-7-signed-releases.md) | [#778](https://github.com/gjovanov/roomler-ai/issues/778) | Signed releases — Windows Authenticode, Linux GPG + provenance, macOS identity | in progress — retrospective; all criteria field-verified except Apple Developer ID (enrolment 5XS5WN8R99 under review); closes on `spctl … Notarized Developer ID` |
| [FR-8](FR-8-claude-session-restore.md) | [#780](https://github.com/gjovanov/roomler-ai/issues/780) | Claude session restore after reboot (`crestore`) | closed — shipped (retrospective); renumbered from FR-7, which #778 claimed 147 s earlier |
| [FR-9](FR-9-lan-pairs-converge-direct.md) | [#768](https://github.com/gjovanov/roomler-ai/issues/768) | Two nodes sharing a LAN converge to a direct carrier | **closed** — all criteria met + field-verified; shipped rc.480 → 0.4.8 (#741 #744 #746 #747 #758 #765 #782) |
| [FR-10](FR-10-relay-drag-quality.md) | [#783](https://github.com/gjovanov/roomler-ai/issues/783) | Relay drag quality — IDR thrift on constrained transports | closed — shipped in 0.4.5, field PASS 2026-08-27 ("CORPLAP-3 from neo16 very smooth"); CORPLAP-1 residual attributed to DERP RTT; kept FR-10 by the lower-issue-id rule vs #784 |
| [FR-11](FR-11-server-grids-members-files-invites.md) | [#784](https://github.com/gjovanov/roomler-ai/issues/784) | Server-side grids — members/files/invites, devices online-first default sort, mesh display names | closed — shipped (#790) + field-verified on prod 2026-08-27; renumbered from `FR-10`, which #783 claimed 104 s earlier |
| [FR-12](FR-12-onboarding-tutorial.md) | [#788](https://github.com/gjovanov/roomler-ai/issues/788) | Onboarding tutorial — guided welcome tour, callable anytime | P1 shipped (#797); **P2 spotlight tours shipped (#1117)** — dependency-free overlay, `data-tour` anchors with a guard test that they still exist (a renamed anchor fails SILENTLY otherwise). ⚠️ The spec's third tour, "forward", is DROPPED not deferred: forwards are made from the CLI/desktop, so there is no web surface to spotlight. **CLOSED — P3 shipped (#1138) + seed fix (#1142/#1147), field-verified on prod**: five new SVGs so no two chapters share artwork (a repeated illustration reads as "I am back where I started"), plus `PUT /api/user/tutorial` mirrored on `/auth/me` so progress follows the person. ⚠️ `done` REPLACES server-side and the client unions on seed — a union on write makes un-ticking inexpressible; `seen` is a one-way latch. ⚠️ `bson::DateTime` serialises as `{"$date":…}`, which is TRUTHY, so a presence check would have passed while a display printed `[object Object]`. 🚨 The seed itself shipped DEAD — it sat behind `maybeAutoOpenTour`'s navigation guards, every automated check passed, and only the field test caught it (#1142); an unrelated PR then silently REVERTED that fix on green CI (#1147 re-landed it) |
| [FR-13](FR-13-rc-keyboard-hid-mapping-mac-chords.md) | [#789](https://github.com/gjovanov/roomler-ai/issues/789) | RC keyboard: unmapped HID usages type garbage (CapsLock→`9`); mac Ctrl→Cmd chords | implemented — viewer half ships with the next web deploy, agent half with the next agent release; field pending |
| [FR-14](FR-14-direct-link-jitter-episodes.md) | [#792](https://github.com/gjovanov/roomler-ai/issues/792) | Direct-link jitter episodes — AIMD sawtooth on VPN-churning links | design — evidence collected (CORPLAP-1 2026-08-27); child of FR-1 |
| [FR-15](FR-15-relay-age-feedback.md) | [#795](https://github.com/gjovanov/roomler-ai/issues/795) | Relay age feedback — close the rate loop with the viewer's own clock | shipped in 0.4.9 (#796); **P2 shipped in 0.4.11 (#804)** — the floor is now bounded by half the viewer own probe RTT, so a clock-biased sample is rejected rather than learned; field gate pending |
| [FR-16](FR-16-rc-quality-benchmark.md) | [#798](https://github.com/gjovanov/roomler-ai/issues/798) | Systematic remote-desktop quality benchmark — the codec/path/device matrix | proposed — hand-testing cannot compare cells (drag speed is itself a first-order input); child of FR-1 |
| [FR-17](FR-17-partial-reliability-video.md) | [#799](https://github.com/gjovanov/roomler-ai/issues/799) | Video rides a reliable+ordered DataChannel — HOL blocking costs SECONDS on a relay | **stages A + B shipped** (#820, #834) — per-chunk framing negotiated via `AgentCaps.video`, then `{ordered:false, maxRetransmits:0}` behind an opt-in. Both INERT until an agent advertising `chunk-framing` rolls; "constrained-only" proved un-implementable (ordering is fixed before ICE) and is recorded as a deviation. Stage C is blocked on a reorder buffer, not on the harness. `send_wait_max_ms` measured at 10 263 ms on a healthy agent; child of FR-1 |
| [FR-18](FR-18-carrier-queue-discipline.md) | [#801](https://github.com/gjovanov/roomler-ai/issues/801) | Carrier queue discipline — the relay path holds seconds of video before the wire | in progress — LAN-relay test came back NEGATIVE (both corp VPNs block LAN on the endpoint), so relaying is structural for those hosts and the queue is the whole lever |
| [FR-19](FR-19-peer-relays.md) | [#805](https://github.com/gjovanov/roomler-ai/issues/805) | Peer relays — tenant-owned UDP relay nodes between direct and DERP | proposed — independently reviewed on 3 lenses pre-publication; carrier is a third `RelayKind` (not a new tier), bind is key-authenticated, **E2E-3 RUN: port settled at 3478** (CORPLAP-3 reaches 3478 and nothing else); P0–P2 and P3a SHIPPED (step log on #805: bind-only responder → forwarding relay field-verified on scw-m2-asahi → models/wire); P3b (#893, DAO/routes/audit/fail-closed loaders) merging; **P3c the mint** (#899), **P4a** the client (#902) + **P4b** runtime/agent wiring (#903) SHIPPED in **0.4.19**, all dark behind `overlay_org_relay` (device) + `peer_relay_mode` (org); **P4c the first field mint next** (Asahi serving, CORPLAP-3 requesting, `warn` then `on`) |
| [FR-21](FR-21-retire-obsolete-binary-names.md) | [#809](https://github.com/gjovanov/roomler-ai/issues/809) | Retire the obsolete `roomler-agent` / `roomler-agent-tray` names — code, docs, file/folder names, all three OSes | **DONE + DEPLOYED** — all 13 acceptance criteria field-verified (2026-08-30), deployment re-measured 2026-08-31: prod API 0.4.30, latest release 0.4.33, **14 of 14 ONLINE agents on 0.4.33**, and the only FR-21 commit not in the latest release touches `CLAUDE.md` alone; the 7 rows on pre-0.4.15 builds are all OFFLINE and dormant, and their wake-up path is the one proven on a throwaway VM. 1 849 → **0** unclassified, 743 anchored; CI blocks on `name-audit.sh --check --strict`; the hard part was the freeze list (7 dual-read anchors, incl. the TCC-keyed macOS bundle) |
| [FR-22](FR-22-time-to-first-frame.md) | [#819](https://github.com/gjovanov/roomler-ai/issues/819) | Time-to-first-frame — connecting sometimes takes 10-15 s | **parts 1 + 3 shipped** (#821) — phase-aware bound (`requesting` 4 s vs ICE's 15 s) cuts the bad case ~18 s to ~7 s, plus eight-mark connect timing so "sometimes 10-15 s" can become a distribution. Part 2 was found ALREADY IMPLEMENTED and recorded, not rebuilt. **3b (#822)** puts the verdict in a SNACKBAR — the console is shut during the sessions that stall, so console-only reporting could never yield a root cause. Root cause still open |
| [FR-23](FR-23-company-identity-and-domain-consolidation.md) | [#827](https://github.com/gjovanov/roomler-ai/issues/827) | Company identity on the site, and one product on both domains | in progress — phases 0–3, **5 and 6 shipped + field-verified**; phase 4 (teardown) **skipped** by the operator, since `janus`/`coturn` on roomler.live still depend on that namespace. #828 #830 #831 #835 #837 #841. Imprint live (`grep imprint` on the shipped bundle 0→1); `roomler.live` now **301s to roomler.ai** (apex only — the domain hosts live unrelated services); Terms + Privacy rewritten to cover all three pillars. ⚠️ **Three false claims were found in legal copy** — the Privacy Policy said the session JWT lives in `localStorage` (untrue since #680), the imprint cited the **repealed** EU ODR platform, and I wrote an absolute security guarantee that has a documented exception: *legal copy invites template drafting; check every claim against code*. ⚠️ `ROOMLER__APP__CORS_ORIGINS` cannot be set from a configmap at all (no `list_separator` ⇒ **boot crash**), which is why the 301 beat serving the app. Open: send Apple the correction. Unblocks the Apple half of FR-7 |
| [FR-20](FR-20-per-tenant-relay-cost-metering.md) | [#807](https://github.com/gjovanov/roomler-ai/issues/807) | Per-tenant relay cost metering — attribute what relay traffic actually costs, per org | P1/P2/P4/P5/P6 shipped + **field-verified on prod 2026-08-30**: 1.77 GB attributed across 2 tenants, **0** negative buckets, **0** loss lines in 24 h, and flush-log == stored value on 3 consecutive minutes. P3 (coturn) BLOCKED upstream — 4.17.2 emits no `user` label. ⚠️ One criterion was **unfalsifiable as written** — `stats_relay` has never stored `net_tx_bytes` (absent in all 100 800 docs) — and is restated against `DERP_BYTES_RELAYED_TOTAL` |
| [FR-24](FR-24-licensing-split.md) | [#838](https://github.com/gjovanov/roomler-ai/issues/838) | Licensing split — AGPL-3.0 control plane, MPL-2.0 everything the customer runs | **shipped (#844)** — AGPL on the server you would host, MPL-2.0 on everything installed on a machine, © G ROX EOOD. ⚠️ The finding that shaped it: AGPL covers ~37% of the tree and **0% of the two cascades the split was argued for** (`crates/api` deps `tunnel-core` unconditionally ⇒ SHARED ⇒ MPL), so it is kept for the **sellable exception**, and `LICENSING.md` says so rather than letting a reader find it in the dep graph. The consent gate was a no-op (one contributor). CI asserts no AGPL crate reaches a shipped agent binary — **verified falsifiable**. LGPL §6 closed by publication: §6(a) accepts the "work that uses the Library" as *source code*, which open-sourcing already provides (`docs/lgpl-relink.md`). Open: CLA needs a lawyer before `cla.yml` is enabled |
| [FR-25](FR-25-call-layout-mentions-pip.md) | [#839](https://github.com/gjovanov/roomler-ai/issues/839) | Call UX: layout controls that honour their labels, mentions, PiP, spotlight + fullscreen | shipped #842 + **#859** + **#873**; **field-verified 2026-08-29 in a real browser**. ⚠️ #842 fixed neither headline defect on its own: the mention crash was FIVE nested installs of prosemirror-model (dedupe, not chunking), and PiP still did nothing in a solo call because the FLOATING self-view had no stream key. Mentions, PiP, self-view in all three layouts, sidebar primary, double-click spotlight and fullscreen all verified. ⚠️ **hide-non-video cannot converge**: producer pause is never signalled to peers, so the receiver sees a live unmuted track — needs its own FR |
| [FR-26](FR-26-rc-viewer-settings-and-metrics.md) | [#840](https://github.com/gjovanov/roomler-ai/issues/840) | Remote-desktop viewer: display name, quality-metric toggles, FSR default, settings reorg | **closed — all criteria field-verified 2026-08-29** in the operator's own browser (display name in the title, metric toggle surviving a reload, FSR ON from a cleared key). Renumbered from FR-24 by the lower-issue-id rule vs #838 |
| [FR-27](FR-27-consent-surfaces-and-desktop-companion.md) | [#854](https://github.com/gjovanov/roomler-ai/issues/854) | Host consent — every mode, every OS, and a desktop companion that works | shipped (#858, #877, #892); **field-verified**: Windows native panel end to end on 0.4.16, `no_prompt_surface` on 0.4.18 + the hub grace. The field test found 4 defects of its own — one froze every pre-0.4.16 Linux agent, one was the hub's own timer discarding the agent's reason. Open: `prompt_owner` (owner account), exec/ssh prompts (`EXEC_DEVICE` grant), two concurrent viewers, macOS + X11 panels |
| [FR-28](FR-28-derp-drain-on-pod-roll.md) | [#865](https://github.com/gjovanov/roomler-ai/issues/865) | A pod roll freezes every relay-carried session — the agent learns the /derp WS died by failing to send on it | **P0 disproven + reverted**; P1 (make-before-break) is the fix — measured 2436 ms delivery gap + decode stall on an IDLE session; chain confirmed on 2 hosts against a 69 s-old pod. The `DerpCancelRegistry` primitive already existed and was simply not wired to SIGTERM |
| [FR-29](FR-29-x11-damage-capture.md) | [#864](https://github.com/gjovanov/roomler-ai/issues/864) | X11 capture reads the whole screen even when nothing changed — the ~27 fps Linux ceiling | **CLOSED** — P1 shipped in **0.4.17** and verified on the released artifact (idle CPU **45.8 %→2.8 %**); P2 merged (#882) for the per-host metric, its rects having no production consumer; **P3 ruled out by measurement** — a window drag reports ONE full-screen rect (`avg_damage_rects=1`, union = bbox = 1000 ‰, compositing on *and* off), so partial readback would read the whole frame anyway. The unmet motion criterion is unreachable from this direction, not unfinished. Spun out #878 (capture path linted by no CI lane) → fixed in #912 |
| [FR-30](FR-30-camera-state-signalling.md) | [#884](https://github.com/gjovanov/roomler-ai/issues/884) | Camera/mic state is never signalled — peers cannot see who turned their camera off | **closed — P1–P4 shipped (#886 #913 #918), field-verified 2026-08-29**. `media:producer_pause` / `producer_paused`, server pauses the mediasoup producer. Tile hides on camera-off and returns with no reload; frames **96 → 0 per 3 s** while the receiver's track reads `enabled:true/live/unmuted` in BOTH states; remote tiles now show camera-off/mic-off with an avatar instead of a frozen last frame. ⚠️ P4 came out of P3's own field test: a pause EVENT reaches only whoever was already in the room, so the state also rides the SUBSCRIPTION (`ConsumerInfo.producer_paused`) — a transition is not a state |
| [FR-31](FR-31-opening-keyframe-budget.md) | [#897](https://github.com/gjovanov/roomler-ai/issues/897) | The opening keyframe of an NVENC session gets one frame's budget — ffmpeg zeroes the VBV in `cq` mode, so the constrained-profile first frame is a 5.6 KB max-QP smear that inter frames repair over ~1.2 s | **P1 (post-open VBV) and P1b (`ldkfs`) both refuted by the A/B**: the opening keyframe is ~1.1–1.3 × `maxrate/fps` (13 KB at 2.55 Mbps, 35 KB at 7.65, 132–140 KB at 25.5) and nothing ffmpeg exposes changes it. Decision pending: relay ceiling (own FR) · vendored-ffmpeg `vbvInitialDelay` patch · accept. #900 draft |
| [FR-33](FR-33-lan-capture-surfacing.md) | [#905](https://github.com/gjovanov/roomler-ai/issues/905) | A VPN that captures the LAN prefix should say so — surface LAN capture in `roomler status`, `peers --json why` and the RC path pill, so a relay on a LAN pair is attributed in seconds, not days | Designed 2026-08-29 after `neo16 → CORPLAP-2` spent five days relay-locked by a Check Point split-prefix capture that every surface reported only as `upgrading` / `relay` / `penalty`. P1 status line field-verified 0.4.20; P2 `why` (`lan-captured` + LAN-tier gate per captured prefix, #1281) field-verified 0.4.59 on 2026-09-04; P3 pill (`rc:video-info.transport_reason`, per-viewer) implemented 2026-09-04, field check pending |
| [FR-34](FR-34-locked-consent-and-stuck-capture.md) | [#917](https://github.com/gjovanov/roomler-ai/issues/917) | A locked host: a consent prompt you cannot see, and a stream that comes up black | Root-caused 2026-08-29 from CORPLAP-1 (locked corp laptop): the FR-27 consent panel is not on the secure desktop, and a DXGI duplication bound during the lock→unlock transition returns empty frames forever (`Transient` never recovers, `capture_pump.rs:562`) — 200k empty / 0 unchanged, black until a reconnect. P1 stuck-capture recovery (gated on frames_delivered==0), P2 first-frame guarantee, P3 consent on the secure desktop |
| [FR-35](FR-35-relay-ceiling-learns-the-pair.md) | [#922](https://github.com/gjovanov/roomler-ai/issues/922) | The constrained ceiling learns the pair — grow the relay cap above the nominal 3 Mbps on delivery evidence (gated by the signals that predicted the stall), remember it per peer so the next session opens there | P0 measured on the REAL `neo16 → CORPLAP-2` relay: at a 25.5 Mbps cap the desktop lands as one 680 KB frame at +0.3 s and the path delivers 3–11 Mbps, but chokes at 12.8 Mbps (7.9 s stall) — sustainable ≈ 6–9 Mbps vs the 3 Mbps constant; other corp relays sit at ~2 Mbps. P1 gated growth + P2 per-peer rate memory designed |
| [FR-32](FR-32-plan-limit-enforcement.md) | [#898](https://github.com/gjovanov/roomler-ai/issues/898) | Eleven of fourteen plan limits are advertised and enforced nowhere | **closed — shipped + field-verified** (P1 of the pricing arc) (enforce → meter → re-price → keys, never inverted). Only `max_devices` (x2) and `max_tunnel_clients` are enforced; the other 11 are published by `/api/stripe/plans` and read back by nothing. Two findings beyond the prior evaluation: `TenantSettings.{max_members,file_upload_limit}` shadow two of them with **zero readers anywhere**, and the `count_*` DAOs exist only for the two limits already enforced — the counting, not the `if`, is the work. Ships in `Warn` (the `acl_mode` precedent) because flipping 11 gates on a live fleet locks out tenants we put over the line | **Renumbered from FR-31**: #897 (opening-keyframe budget) claimed it with the lower issue id, and the lower id keeps the number. FR-32 was verified free rather than vacated (`git log -S "FR-32"` on the ledger returns nothing, and no issue was ever titled FR-32) — a vacated number must never be reused  **Closed 2026-08-30** on `v20260830-eaba9f4b0554`: 9 of 11 dead limits wired, 2 withheld with reasons (`cloud_integrations` has no implementation to gate; `max_concurrent_sessions` has no table — `RemoteSessionDao::create` has zero callers). P2 complete for existing tenants (65/65 `Enforce`). ⚠️ **The default stays `Warn` by standing operator decision (early growth), so NEW SIGNUPS ARE NOT ENFORCED** — recorded on the enum so it is not "fixed". 🔑 The P1c report initially answered `would_break: 0` while omitting the video gate; Free's cap was 0, so acting on it would have removed conferencing from every Free tenant — a report used to AUTHORISE a rollout must cover every gate that rollout turns on. Also: Free video 0→4, AI recognition deleted (−643 lines, verified unused first), and 63 of 65 tenants are test debris so the field data is not a pricing input
| [FR-36](FR-36-wayland-capture.md) | [#929](https://github.com/gjovanov/roomler-ai/issues/929) | Wayland capture, and unattended access | **✅ CLOSED 2026-08-30 — every functional criterion field-verified.** The browser renders a GNOME Wayland desktop, **drives it, TYPES into it**, sees the **GREETER** with nobody logged in and a **LOCKED** screen, and **survives a reboot** — DRM/KMS capture + `uinput` input, both **opt-in**. Gates come **from CONFIG not env**, which is what let them survive the reboot and reach a daemon started outside systemd. 4K `Auto` **52.9 → 24.0 ms** (~30 fps) once the downscale was fused into the repack. **`apple-drm` advertises ONLY `MOD_LINEAR` ⇒ P2 detiling unneeded on Apple Silicon.** ⚠️ Two numbers did NOT meet their bar and are recorded, not reworded: 4K undownscaled is 43.8 ms (memory-bandwidth bound). ⚠️ Typing needs a **detected + verified** layout (`us`) — `libxkbcommon` rejected because a dynamic `DT_NEEDED` would stop headless daemons booting. ⚠️ Naive downscale fusion was **6× WORSE** — scanout reads are sequential-vs-strided, not pass-count. ⚠️ The host's 5-day restart storm was **two enabled systemd units**, and `systemctl start roomlerd` was itself triggering it. **`libdrmtap` MIT, RustDesk AGPL-3.0 — never copy its source** (FR-24) |
| [FR-37](FR-37-e2e-in-cluster.md) | [#928](https://github.com/gjovanov/roomler-ai/issues/928) | The e2e suite runs outside the cluster, so it cannot test media | **closed — P1–P3 done, field-verified.** The browser is now a SIDECAR in the app pod (127.0.0.1 is a secure context; a Service URL is not, so `navigator.mediaDevices` is undefined there). Nightly: **165 passed / 2 failed / 6.5 min**, from 154 / 13 / 17.8 min — and the only failures left are the two `rc-vp9-444` specs, which need an agent. ⚠️ The last blocker was a PRODUCT bug: a blank `turn.url` made calls unjoinable in any browser (#940). ⚠️ A "Job" cannot share the app's network namespace — that is why it is a sidecar |
| [FR-38](FR-38-idle-keepalive-spend.md) | [#949](https://github.com/gjovanov/roomler-ai/issues/949) | A static screen costs 0.3–0.9 Mbps on a relay — the idle keepalive re-encodes an identical frame for 3–6.5 KB | P0 open — measured on CORPLAP-2/3 (0.7–1.3 KB/frame at rc.474 → 3.5–6.5 KB at 0.4.20, identical encoder options); bisect on the local relay rig next |
| [FR-39](FR-39-launch-readiness.md) | [#951](https://github.com/gjovanov/roomler-ai/issues/951) | The product is unfindable — repo metadata, README, comparison docs, and a way for a visitor to stay in touch | **P1-P6 implemented** — measured 2026-08-29: the repository description still sold the pillar #490 demoted to third (no mention of remote desktop or mesh), topics carried none of the terms this audience searches, **0 stars on 577 releases**, the landing page had no email capture, and search still returns the retired `roomler.live` identity. ⚠️ The finding that reshaped the plan: **there was no self-host path at all** — `docker-compose.yml` brings up dependencies only and never ran the application, and no workflow publishes an image anywhere, while `LICENSING.md` promises self-hosting on unlimited devices forever. Shipped: repo metadata + 20 topics (live), `docker-compose.selfhost.yml` + `docs/self-hosting.md`, a README that leads with it, `docs/compare/` x5, and a `subscribers` collection behind three public routes whose **202 is identical for a fresh and a known address** (a membership oracle against `users.email` otherwise). **CLOSED 2026-09-01 — every acceptance criterion met.** The three items this row listed as open are done: the demo is shot and on the README's first screen (#1122), the social preview is uploaded, and the clean-box run became FR-42. 🔑 Shooting it found that **the harness would have filmed a lie** — ping targets were hardcoded to the pre-FR-47 block, so the take would have filmed two unreachable pings and reported every scene `ok`, because a scene is *no exception and no hang*, never *the content is right*; they now resolve by node name at record time. ⚠ A take also reported five `scene ok` lines while filming our own crash-loop warning on a healthy host — unnoticed for seven releases, and surfaced only because a camera was pointed at the machine (FR-53) |
| [FR-40](FR-40-overlay-key-rotation.md) | [#962](https://github.com/gjovanov/roomler-ai/issues/962) | Rotate a device's overlay key from the dashboard — the server orders a re-mint it never sees | P0 spec; P1 in implementation — exposure bounded to ONE device / ONE key by a masked transcript scan (no token, no SSH host key). Reuses the existing re-join → upsert fan-out → peer reinstall path (`RuntimeFingerprint` already includes the pubkey; `key_epoch` plumbed since Phase 5 and read by nothing); adds the cap-gated order, the device's mint-persist-report-reconnect, reconcile-on-connect, audit + ceiling, the pubkey on the device row, and (P2) retired keys refused at join/DERP with a self-heal order |
| [FR-41](FR-41-product-demo-recording.md) | [#965](https://github.com/gjovanov/roomler-ai/issues/965) | A 90-second demo of the product actually working | P0 — child of FR-39. The one launch asset nothing substitutes for, and the existing `roomler-intro.mp4` is a 2:24 **collaboration** walkthrough that predates the #490 pivot, so linking it confirms the stale positioning a web search already returns. Staged as: neo16's browser (Playwright) → the **WSL2 sibling** enrolled into a fresh `demo` org on prod. ⚠️ NOT neo16 itself — removal from GROX is irreversible (tombstoned, never gets `100.65.4.2` back) and a browser controlling its own host is an infinite mirror in the one scene the video exists for. Reuses `ui/e2e/video/` rather than building a harness. **CLOSED 2026-09-01** — 89.4 s, Windows 11 ↔ MacBook, 4/4 replies each way at 9 ms / 6.3 ms and 0.0% loss, verified by extracting frames and looking at them |
| [FR-42](FR-42-selfhost-verified-on-a-clean-box.md) | [#967](https://github.com/gjovanov/roomler-ai/issues/967) | Self-hosting, actually watched coming up on a clean machine | P0 — child of FR-39, which shipped `docker-compose.selfhost.yml` + `docs/self-hosting.md` **without anyone running them**. What was verified was that the compose *renders*, fails fast on a missing secret, and sets only keys that exist in `settings.rs` — none of which is a run. ⚠️ This is the exact path a launch post sends strangers down, and r/selfhosted's rules require production-ready-with-docs; a failing `up` is a launch-ending comment in public. Two claims in that doc's first draft were already wrong and caught only by reading code. On Docker Desktop + WSL2 |  **CLOSED 2026-09-01 — P3 published and measured.** `ghcr.io/gjovanov/roomler-ai` is live (`v0.4.43` + `latest`, amd64, 175 MB), anonymously pullable, and the documented clean-box path is **88 s** (pull 20 + up 68) against **359 s** to build on a 16-core box. Workflow is dispatch-only and SMOKE-TESTS before it pushes — the container must serve `/health` AND the SPA, and the bytes pushed are the bytes tested. 🔑 A tag and a binary version are different claims and they DRIFT while a 20-minute build runs: the dry run was dispatched `v0.4.44` and built a binary reporting `0.4.42`. ⚠️ `docker compose up -d` ALONE BUILDS, it does not pull — the docs run `pull` first and say why. 🔑 The enrolment criterion stays `[~]`: a device DOES enrol against the self-hosted server using the value from the SERVED script, but "online" needs a host with no daemon (one machine serves one). ⚠⚠ An isolated `--config` does NOT isolate the UPDATER — a probe made the host self-update system-wide and left the running daemon on the DELETED inode
| [FR-43](FR-43-macos-single-enrollment.md) | [#971](https://github.com/gjovanov/roomler-ai/issues/971) | One macOS device row — a supervising daemon and an unenrolled GUI worker | **P0 + P1 complete, field-verified** (0.4.33 → 0.4.36; P2 next) — two PROCESSES are forced by macOS (no `SetThreadDesktop` equivalent, so a session-0 daemon can never reach WindowServer), two ENROLLMENTS are ours (`hub.rs:89` displacement). Ports the Windows supervisor shape (`spawn_in_session`/`decide_spawn`) to `launchctl asuser` + the LocalAPI socket. ⚠️ The FR-5 cert does NOT enable this — TCC *persistence* ≠ *session access* — but it is what makes the refactor safe to attempt. ⚠️ Every P1 field failure was ONE root cause: whoever loses the agent's single-instance lock exits **0**, and `KeepAlive{SuccessfulExit=false}` never retries a clean exit (#1029 orphan, #1039 empty hand-back, #1040 spurious rollback) |
| [FR-44](FR-44-shared-pipeline-multi-viewer.md) | [#1024](https://github.com/gjovanov/roomler-ai/issues/1024) | Shared-pipeline multi-viewer — a second viewer of one screen rendered black, then dropped the first | **CLOSED — retroactive; three causes, three fixes, field-verified (0.4.31/0.4.32/0.4.33).** (1) join-IDR dropped before the follower video DC opened ⇒ `resync_follower` on DC-open (#1015). (2) THE real black cause: the follower chunker sent RAW un-chunk-framed bytes while the browser expects FR-17 framing ⇒ `FollowerSink.chunk_framing` + chunker mirrors the owner (#1018, byte-level verified). (3) shared egress was not viewer-count-aware ⇒ a 2nd viewer oversubscribed the constrained relay and the leader ICE-dropped ⇒ `shared_split_ceiling_bps` divides the ceiling by live viewers (#1020, target_bps 2.55M→1.5M). 🔑 the pipeline double-TRANSMITS over one relay, it does not double-render. Out of scope: the base latency (corp-VPN LAN-capture relay lock, FR-33) and full quality for both (needs server-side fan-out) |
| [FR-45](FR-45-portal-capture.md) | [#1041](https://github.com/gjovanov/roomler-ai/issues/1041) | Portal capture: Wayland where there is no scanout | **P1 (detection) SHIPPED; the P2 blocker is DIAGNOSED and CLEARED.** The case FR-36 cannot reach: the only host that can CAPTURE Wayland (Asahi) has no video-encode driver, and the only one that can ENCODE in hardware (WSL2, `av1_nvenc`, 10.4 ms) has NO `/dev/dri`. 🔑 **The portal exposed nothing because `xdg-desktop-portal-gnome` was `inactive (dead)` — a static D-Bus-activated unit nothing had triggered — and starting it alone did NOT help: the FRONTEND caches its backend selection at startup and must be restarted after it.** Now `available (screencast v5, remote_desktop=true)`. FR-36 blamed an X11 session for this and that was wrong. ⚠️⚠️ **P1 also found the daemon runs as ROOT with no session bus** — the portal is per-user-session, so P2/P3 need a session-resident component before any PipeWire code. ⚠️ Detect at SESSION TIME, never cache: a host can have every package installed and still offer nothing depending on start order. ⚠️ ATTENDED ONLY. ⚠️ `libpipewire` would land in `DT_NEEDED` and stop headless daemons STARTING — dlopen or a helper before direct linking (zbus is pure Rust, no such hazard) |
| [FR-46](FR-46-eradicate-the-retired-agent-name.md) | [#1051](https://github.com/gjovanov/roomler-ai/issues/1051) | Eradicate the retired agent/tray/tunnel binary names (spelled out in the FR-21 row above) as LIVE names — the surfaces FR-21 deliberately froze, now that re-enrollment is authorised | P0 spec. **Not FR-21 reopened**: FR-21 succeeded under *migrate-or-anchor with no disruption*; FR-46 changes exactly that one input. Two findings reshape the plan — (1) the anchor set is **two populations**, live-compat vs historical record, and only the first can or should reach zero, so P1a splits the marker and guards them differently (`live` ratchets down, `record` pinned exactly, so nothing can be laundered into permanence); (2) **the published-asset freeze is far narrower than D6 recorded** — Windows keys on `.msi`+`-permachine-`, macOS on `.pkg`, Linux >=0.4.16 already dual-matches `roomlerd`, and the server-side ordering is a companion DENYLIST, so `roomlerd-*` can ship without freezing anything. The one genuine blocker is unchanged: the macOS bundle keys the TCC grants, and that is re-CONSENT (a human at each Mac), not re-enrollment |
| [FR-47](FR-47-overlay-multi-block-ipam.md) | [#1071](https://github.com/gjovanov/roomler-ai/issues/1071) | One address space per org — carve by default, grow without renumbering | **shipped — all 8 phases; the isolation half FIELD-VERIFIED on prod, the growth half shipped DARK.** Carving was OFF in production (no `ROOMLER__OVERLAY__*` key in the configmap), so isolation was opt-in and nobody opted in: **2 of 4 networks shared `100.64.0.0/10` and held overlapping addresses**. Now every org has a disjoint block, **0 live nodes remain on the legacy `/10`**, and 17 leaked host ordinals were reclaimed. 🔑 A default that has to be remembered is not a default. ⚠️ P5a–e multi-block is behind `overlay.multi_block_enabled` (off): a real **pre-P5d agent binary** against a grown org is still unverified, and no org is near its 1 022 ceiling (largest: 17 devices). ⚠️⚠️ `ensure_indexes` must tolerate `NamespaceNotFound` (26) as well as `IndexNotFound` (27) — 26 is every fresh DB, so tolerating only 27 breaks the first clean install and nothing else. ⚠️⚠️ Block order is an explicit `seq`; `slot` and `created_at` both lie after a reclaim |
| [FR-48](FR-48-roomlerd-ice-socket-leak.md) | [#1086](https://github.com/gjovanov/roomler-ai/issues/1086) | roomlerd leaks WebRTC-ICE UDP sockets on the relay node (surfaced by the FR-19 box-886 census) | proposed — 0.4.33: **+3/h monotonic climb (16→64 over 15 h)**, reset only on reconnect; all ephemeral `0.0.0.0:<rand>` UNCONN = the ICE host-candidate class (same as the 2026-08-22 `TunnelPeer` leak, uncovered path); NOT org-relay-specific (climbed while org OFF + idle) → 0.4.23→0.4.33 correlation. Prime suspect = the carrier direct-upgrade probe leaking a socket per re-attempt. P1 = RUST_LOG on a node + audit the WebRTC paths; P2 = `close()`+`Drop`, census flat over 24 h (closes FR-19 F6) |
| [FR-49](FR-49-second-org-has-no-mesh.md) | [#1084](https://github.com/gjovanov/roomler-ai/issues/1084) | A second org gets no mesh, and every surface reports normally | P0 spec. Found enrolling FR-41's demo devices: `enrollment.rs:328` forces `overlay_mode: Off` on every appended org, and `--overlay` (`main.rs:1653`) sets the **primary's** flag whichever identity was enrolled — the flag reads as granted and applies to the wrong org. ⚠️ The reason it costs days rather than minutes is that **five surfaces all report normally**: the enroll output never mentions the mesh, `org ls` has no overlay column, `status`'s `OrgStatus` has no overlay field (the org's WS *is* connected), `peers` omits an overlay-off org **entirely** — indistinguishable from one whose peers are offline — and the dashboard only knows what the server was told. The `Some([])` vs `None` lesson from the ACL work, and `ssh_activity`'s "empty ≠ inactive", recurring on a third surface. ⚠️ There is also no supported way to turn it on: `roomlerd org` has `ls/rm/enable/disable/set-primary` and nothing that touches `overlay_mode`, leaving hand-editing the token-bearing `config.toml` as the only path |  **CLOSED 2026-09-01 — all eight criteria field-verified on 0.4.41.** Dark-org surfaces proven live on a two-org host (`peers` names it instead of omitting it; `status` shows `connected, overlay OFF` on ONE line — the whole defect in one row), and `org overlay demo tun` + restart brought the mesh back with pings at 68/81 ms. 🔑 Found a SIXTH silent surface the spec missed: `roomler status` never rendered `NodeStatus.orgs` at all — on the wire since multi-org P1, shown only by `--json`. 🔑 CI runs `--lib` and never `--bins`, so the locks had to move out of `main.rs` to be able to fire. 🔑 Three of the four overlay-flag tests still PASS against the pre-fix code — only one catches the defect
| [FR-50](FR-50-installer-knows-its-server.md) | [#1083](https://github.com/gjovanov/roomler-ai/issues/1083) | The installer cannot know which server handed it to you — a self-hosted install enrols against the hosted service | P0 spec. Found by FR-42's clean-box run: `scripts/install.sh:46` and `install.ps1:64` carry a compile-time `https://roomler.ai`, and the route serving them (`setup_release.rs:170-189`) takes **no state and no request**, so a piped script cannot see its own origin even in principle. Fix is serve-time substitution from `app.frontend_url` — already the canonical "which server am I" for OAuth returns, invite links and the CORS origin policy, so a deployment that gets it wrong is broken before the installer is. ⚠️ The substituted value lands in a script that runs as root/SYSTEM, so it is validated to `scheme://host[:port]` and a refusal serves the compiled-in default unchanged. ⚠️ Verified a **byte-for-byte no-op on prod** before writing the code (`FRONTEND_URL: https://roomler.ai`) — the population that could regress is the one whose bytes do not change. ⚠️ **Renumbered from FR-48**, which #1086 claimed on master while this claim was in flight: the LEDGER arbitrated (a claim that never reached master moves), and the lower-issue-id repair applies only to two claims that BOTH landed — using it here would have forced a merged FR to renumber, which is the churn that rule exists to prevent |  **CLOSED 2026-09-01 — verified on PRODUCTION**: `curl https://roomler.ai/api/setup/install.{sh,ps1}` are byte-identical to the committed scripts (the intended no-op there), while a self-hosted instance serves its own origin. Injection refusal and the needle lock both proven by making the change and watching them fire. 🔑 The instinct is a docs fix and FR-42 shipped one; it does not hold, because the failure it prevents points away from its own cause
| [FR-51](FR-51-ephemeral-nodes.md) | [#1095](https://github.com/gjovanov/roomler-ai/issues/1095) | Ephemeral nodes — a device that removes itself (Tailscale-class), for CI runners, containers and autoscaled workers | **field-verified on prod 0.4.46, 2026-09-01** — all ten ACs ran against real docker containers on the build host, fail-first shown on the prior image (self-unenroll 404, no `ephemeral` in the device feed, an expired row unreaped). SIGKILL→reaped ~2 min (`silent=118s/ttl=60s`); `docker stop`→gone ~2 s; one key + ONE forced hostname ⇒ 3 simultaneous devices; revoked key refused by name with ~23 h expiry left; addresses recycled twice (`free_hosts=[2,1]`); `agents` ended at its exact 63/33 baseline after NINE ephemeral lifecycles (zero tombstones — the hard delete is real); the 5 long-silent permanent sentinels survived a live reaper; the reap cycle ran on exactly one of two flagged pods; the lifecycle ledger stamped every removal with its true path. Bonuses caught live: netstack containers promoted a direct LAN carrier, FR-47 carved the fresh org its own `/22`, and the Free device cap filled with dead runners until the reaper freed slots — F5 demonstrated by accident. Residuals: AC5's ≥24 h re-read (flag stays on), and the non-affine `removes` fan gap → **#1186** (measured both arms; recycle safety verified intact). ⚠️ Runbook: a configmap-only change does NOT restart pods — one pod had the reaper flag, one did not, until a `rollout restart` |
| [FR-52](FR-52-cross-org-remote-access.md) | [#1100](https://github.com/gjovanov/roomler-ai/issues/1100) | Cross-org remote access — an outsider views/controls a device with a device-held password, consent and audit (the TeamViewer/AnyDesk gap `docs/compare/vs-teamviewer.md:63` concedes in public) | proposed — the org boundary is not a policy, it is the shape of the only authorization path: `resolve_session_authz` (`ws/remote_control.rs:1708`) reads `get_member_permissions(agent.tenant_id, …)`, a non-member gets `0`, and the `REMOTE_CONTROL` check refuses at `:1804`. The design is decided by ONE constraint, not by the feature: the last gate must be device-owned, as `remote_config_enabled` is (`models.rs:915` — "deliberately ABSENT and must never be added"), so the tempting build — POST the password, server compares a hash on the `agents` row — is the one that must not ship: it makes a DB dump a fleet-wide credential dump. Password lives ONLY on the device (`ssh_host_key` treatment: atomic+fsync+`.prev`+0600, and unpersistable ⇒ stay OFF), verified by an OPAQUE aPAKE the server blindly relays, with the derived key BOUND to the DTLS fingerprint at offer time — without that binding the server is still a MitM and "the device verified it" buys less than it sounds like. Five gates (org switch → admin approval → device opt-in → proof → consent); ⚠️ **no free permission bit** (`VIEW_SSH_AUDIT` = 1<<30, `ALL` = (1<<31)-1, UI mask signed int32) ⇒ approval is compound `MANAGE_AGENTS`+`REMOTE_CONTROL`, the FR-19 shape. Four findings: the hub keys controllers by `ObjectId` so an anonymous principal forks session/audit/TURN/rate-limit; a user with NO org is already legal (`auth.rs:108`) so an account costs one signup and no new type; TURN creds key on the controller's user id and an external controller has no tenant ⇒ FR-20 must meter to the DEVICE's org or under-bill every external session; and `local_relay` is validated as *an* overlay address, not *the caller's* (`hub.rs:58`,`:1167`) ⇒ it is a probe into the org's mesh from outside it unless rejected outright |
| [FR-53](FR-53-stale-rollback-sentinel.md) | [#1123](https://github.com/gjovanov/roomler-ai/issues/1123) | A recovered device warns about a crash loop forever | P0 spec. Found by accident while shooting FR-41's demo — the machine on camera was displaying it. `clear_attention_on_healthy_connect` (`notify.rs:176`) clears every sentinel reason except `rollback_failed`, and its comment is right about the case it reasons over ("a successful connect of the ROLLED-BACK binary disproves nothing") — but that is not the case that occurs. The device updates again, the bad build is gone, and the sentinel keeps asserting a crash loop in a version no longer installed, recommending a manual reinstall that would downgrade a working host. Measured: `macbook-pro` claiming "0.4.34 has crashed 3 times" while running **0.4.40/0.4.41** and answering the mesh 2 s earlier. ⚠️ Nobody reported it; every device that ever survived a bad update is showing it. Fix = record the failing version in the sentinel and clear once the running one differs. 🔑 Legacy sentinels (all of them today) need no parsing: one with no `Failed-version:` line can only have been written by a binary older than the one introducing the field, so the writer is provably not the runner |
| [FR-54](FR-54-orphaned-tenant-detection.md) | [#1130](https://github.com/gjovanov/roomler-ai/issues/1130) | An organization can vanish and leave its mesh behind | proposed — found during the FR-47 audit: a tenant row was gone while its overlay network, 2 nodes and 2 agents lived on, holding `100.64.0.1`/`.2` — **the very addresses `demo` also held**, so the collision FR-47 opened on was caused by an orphan nobody could see. ⚠️ Correction to the first framing: there is **no tenant-delete code path at all** (only `set_archived`), so this was manual Mongo surgery, not a broken cascade. Therefore a DETECTOR (`POST /api/admin/overlay-network/orphans`, platform-operator, dry-run default, releasing through `release_overlay_node`) and deliberately **not** a tenant-delete route — make surgery detectable, not supported |
| [FR-55](FR-55-always-reachable.md) | [#1154](https://github.com/gjovanov/roomler-ai/issues/1154) | A device stays reachable instead of quietly sleeping | proposed — the product has NO opinion about power: `IOPMAssertion` / `SetThreadExecutionState` / `PowerCreateRequest` / `systemd-inhibit` have **zero** occurrences across `agents/` and `crates/`. Measured on the MacBook: idle timer `sleep 1` (one minute), and it really sleeps on AC (`Entering Sleep state due to 'Idle Sleep'` / `'Clamshell Sleep'`). The only `roomlerd` assertion is an `iohideventsystem` tickle — a byproduct of INJECTING INPUT, so it protects a session where someone is typing and not a view-only one. ⚠️ Two families of answer are both needed: don't sleep (assertions) AND wake it (magic packet from a mesh peer on the same L2; `womp` is already on). ⚠️ macOS **clamshell** sleep defeats any idle assertion — an OS limit the UI must state rather than appear broken. ⚠️ Default OFF: draining a laptop battery is user-hostile, so `power_policy = never/on-ac/always` is device-owned, and P1 is the narrow always-correct piece — an ACTIVE session holds an assertion |
| [FR-56](FR-56-remote-apps-on-wayland.md) | [#1157](https://github.com/gjovanov/roomler-ai/issues/1157) | Remote Apps on Wayland — per-app streaming, and the RAIL circle | **P1/P2/P4/P5 shipped and field-verified (#1162, #1173, #1179); P3 REFUTED (#1175)** — two measurements closed off whole directions rather than costing a phase each: (a) GNOME does not merely LACK window enumeration, it **refuses** `Introspect.GetWindows` (`Access denied`, two independent clients), so mutter's `RecordWindow` has no id to take and per-window capture is the portal's own PICKER only — **attended by construction**, which is why `ROOMLERD_WINDOW_CAPTURE` defaults off; (b) a Wayland-NATIVE window is **invisible to `wmctrl`** (measured: `konsole` ran, was unlistable; `xterm` via Xwayland listed instantly), so preferring a Wayland terminal — or exporting `WAYLAND_DISPLAY` into launched apps — would trade a *visible* failure for a *silent* one. P5 therefore shipped the honesty the same session exposed instead: `apps supported: true` on a host with **no `tmux`**, so `Coverage.missing_tools` now names each absent helper before the click. Remaining work needs hardware or a human (a wlroots host; somebody at the screen to answer the picker). Original framing: closes the loop FR-45 opened: a Wayland desktop is now capturable, while **Remote Apps is X11-only and gated on the DAEMON's own `DISPLAY`** (`apps/mod.rs:152` is literally `env::var_os("DISPLAY").is_some()`), so on a Wayland host it does not fail — it never engages. 🔑 The circle closes CHEAPLY for **Xwayland** apps: mutter runs Xwayland even headless, and with `DISPLAY` **plus `XAUTHORITY`** (`linux.rs:58` sets only the former; without the cookie `wmctrl` dies `Authorization required`) a launched window was listed AND focused, rc=0. FR-45's `companion::graphical_session()` already discovers the session, so P1 is plumbing. ⚠️⚠️ For **native Wayland** windows it closes only where the compositor exposes a foreign-toplevel protocol: measured, mutter offers **none** (`xdg_activation_v1` is token-based SELF-activation, not third-party control) and bare mutter has no `gnome-shell` ⇒ no `org.gnome.Shell.Introspect` — so GNOME can be listed at best, never focused, and `foot`/`gnome-text-editor` were alive and INVISIBLE to `wmctrl`. That asymmetry ships as a REPORTED CAPABILITY (`sources`, not a bool — the `Some([])` vs `None` lesson on its fourth surface) rather than being rediscovered per host. P4 is the RAIL payoff (portal `types=WINDOW` / mutter `RecordWindow`), reusing the whole FR-45 P3 pipeline with only the source swapped, as P5 did. ⚠️ **Renumbered from FR-55**, which #1155 landed while this was in flight — the LEDGER arbitrated (the claim that never reached master moves), not the issue ids |
| [FR-57](FR-57-arm64-selfhost-image.md) | [#1161](https://github.com/gjovanov/roomler-ai/issues/1161) | An arm64 self-host image | P0 spec. FR-42 shipped amd64 and ruled arm64 out for a reason that was about **QEMU, not arm64** — cross-building mediasoup takes hours and an arm64 image nobody has run is a worse promise than none. GitHub's `ubuntu-24.04-arm` runners build natively and can smoke-test on the arch they built for, which removes that reason. 🔑 One job per arch on its own native runner, pushed **by digest**, joined by `imagetools create` — two jobs cannot both push one tag, the second silently wins and the manifest names one arch. ⚠️ A failing arm64 leg must not block the amd64 publish (published, documented, measured at 88 s). ⚠️ The runtime image ships only `roomler-ai-api` + `derp-relay` + the UI, so the risk is `mediasoup-sys` (meson/ninja + vendored openssl), the vendored webrtc tree and `ring` — none known to fail on aarch64, none tried. The first build IS the experiment |  **CLOSED 2026-09-01 — published multi-arch.** `ghcr.io/gjovanov/roomler-ai:v0.4.45` + `latest` are manifest lists carrying `linux/amd64` and `linux/arm64`, verified against the registry with an ANONYMOUS token. 🔑🔑 **arm64 built FASTER than amd64** (15m33s vs 20m23s on `ubuntu-24.04-arm`), and the three dependencies this spec named as the risk — `mediasoup-sys`, the vendored webrtc tree, `ring` — all built on aarch64 with NO change. Every reason FR-42 gave for deferring was about QEMU, not arm64. ⚠️ One criterion stays `[~]` and is split precisely: RAN on arm64 (the native runner is a real aarch64 host and served /health + the SPA), but never PULLED on one — `fedora-arm` sits on a mesh this workstation is not in. 🔑 Shape worth reusing: push BY DIGEST per arch then `imagetools create` (two jobs pushing one tag = the manifest names ONE arch), `fail-fast:false` + merge on `always()` so the new arch cannot fell the proven one, per-arch cache scopes, and a warning when only one arch lands
| [FR-58](FR-58-newsletter-sending.md) | [#1170](https://github.com/gjovanov/roomler-ai/issues/1170) | Newsletter sending — the list exists, and nothing can mail it | **CLOSED — field-verified on production 2026-09-02** (deploy `v20260901-bfc5f58ef358`): a real click on the live form → 202 → the FIRST subscriber row the collection ever held → confirmation in the Gmail INBOX → issue #1 sent from its `.md` (`completed {total:1, sent:1}`, delivered as "Roomler Field Notes") → RFC-8058 one-click with the real token (200 ×2, idempotent) → suppression proven (probe `{total:0}`, zero rows) → dashboard card re-subscribed the account (`source: account`), profile toggle ON. ⚠️ Field finding: **GET-confirm is prefetch-confirmable** — Gmail's link scanner confirmed the row before any human click; follow-up = confirm-on-page POST (the consent-route shape, third surface). P0–P5: #1171 #1176 #1185 #1188 #1189 #1190. FR-39 shipped the list and deferred "newsletter *sending*, templates" by name (`FR-39-launch-readiness.md:139`); this is that program, and it opened on a measured zero: the landing capture has been dead END-TO-END (`StayInTouch.vue:75` posts `'/api/subscribe'` through a client whose `BASE_URL` is already `/api` ⇒ the wire request is `POST /api/api/subscribe` ⇒ 404 for every visitor — and no test layer could see it: integration hits the API, unit mocks the client, no e2e drives the form), and the confirm/unsubscribe 303 lands on the auth-gated `/` (`router.ts:255` exact-matches `fullPath === '/'`) so a logged-out confirmer bounces to `/login` and the outcome handler (`LandingView.vue:320`) is dead code. Design: claim-first per-recipient ledger (unique `{issue_id, subscriber_id}` — 🔑 the INDEX is the invariant, the issue claim only an efficiency gate), stuck claims REPORTED never auto-retried (`retry_stale` is an operator decision), terminal status `Completed` never `sent` (counts carry the truth; a provider 202 ≠ delivered), pre-render + `%%UNSUBSCRIBE_URL%%` so preview IS the sent artifact, RFC-8058 one-click POST method-routed onto the existing token path, pulldown-cmark with raw-HTML events dropped structurally, `require_platform_admin` 404-on-miss as the whole gate, and a 30 s per-send timeout that is load-bearing (the SendGrid reqwest client has NO default timeout — one hung connection would wedge the fan-out with a live heartbeat masking it). Rejected with reasons recorded: `background_tasks` (bare spawn, tenant-scoped, no retry/recovery) and Redis `try_claim` (skip-a-cycle ≠ must-send-once). Post copy lives in the private annex per FR-39's own scope rule |
| [FR-59](FR-59-slow-link-latency-priority.md) | [#1163](https://github.com/gjovanov/roomler-ai/issues/1163) | Slow-link latency priority — make delay the controlled variable | in progress — field 2026-09-01 (CORPLAP-3 → neo16 over a phone hotspot): the agent pushed **1.5–2.1 Mbps into a MEASURED 0.4 Mbps pipe** and, because its own queue read empty (`bytes_inflight` 1–4 KB, `send_wait_max_ms` 0.1 ms — the queue lives in the relay and the carrier, not the agent), the AIMD **climbed** while viewer paint age ran 597–7,095 ms. 🔑 A floor is an assumption about the BAND, not physics: `MIN_BITRATE_BPS` 1.5 M is right for a 2–9 Mbps relay and 3.8–23× wrong here, and it IS the AIMD floor, so no other lever can reach the pipe. 🔑🔑 A queue budget in MILLISECONDS is a lie unless the bps it divides by is the pipe: `constrained_queue_budget_bytes(relay_max_bps())` = 168,750 B = **3.4 s** at 395 kbps, so the byte gate never fired (`frames_dropped_backpressure=0`). 🔑🔑 P3 — the only signal that CANNOT be fooled comes from the receiver: `rx_bps` + a queue-growth **derivative** measured as `Σ(Δarrival − Δwire)`, a difference of intervals in which the clock offset cancels, so it still speaks where FR-15 age is absent/implausible (8 of 14 windows). ⚠️ Consuming a lumpy under-estimate for the QUEUE BUDGET is safe where consuming it for the CEILING is not (under-estimate ⇒ smaller budget ⇒ more shedding ⇒ LOWER latency) — which is why P2 may use the signal `governor.rs:299` correctly refuses. ⚠️ `rx_bps` is a LOWER BOUND on capacity, so it may bound the ceiling ONLY while the queue is growing. ⚠️ **Renumbered from FR-57**, which #1161 landed on master while this was in flight — the LEDGER arbitrated (the claim that never reached master moves), not the issue ids. 🚨🚨 **0.4.47–0.4.49 REGRESSED THE FLEET** (2026-09-02: CORPLAP-1 p90 2–11 s and max 22 s on the relay, against p90 70 ms a week earlier): the `coarsen_bitrate` LADDER bottomed at 1.5 M — a second copy of the pin P1 removed from the AIMD — so **no relieved target ever reached an FFmpeg encoder** (every frame exactly 1.5 M ÷ fps ÷ 8 bytes while the heartbeat said `target_bps=200000`), P5's 15 fps then made each pinned frame 4× fatter, its 1280 cap fought the idle refine (a native IDR each way on every heartbeat), and a slow FR-35 memory never reached the opener because the learner only RAISES. 🔑 The harness could not see any of it: netem shapes below the socket, the WSL agent has no QSV and no pair memory. **P8** (0.4.50) extends the ladder, opens at the remembered rate, makes the low-band additive step relative, holds the refine under the profile. Found alongside and fixed in the same release: **webrtc-sctp 0.11.0's FORWARD-TSN parser read past its chunk**, so every packet Chrome bundled a SACK behind a FORWARD-TSN in was dropped (549 lost acks in one day on one host) — vendored with a CI drift gate |
| [FR-60](FR-60-public-docs-site.md) | [#1165](https://github.com/gjovanov/roomler-ai/issues/1165) | Public documentation site at roomler.ai/docs — static, searchable, per-OS | proposed — the product has **no public documentation site at all**: `/landing` sells but teaches nothing, the FR-12 tutorial is **behind auth** (invisible to a crawler and to anyone still evaluating), and `docs/*.md` is the right depth in the wrong voice for the wrong audience. Four gaps measured, not assumed: **no `robots.txt` and no `sitemap.xml` exist anywhere in the repo**; **`ui/index.html` has no SEO metadata at all** (`<title>Roomler</title>` and nothing else, so all five public routes share one title and no link renders a card); and **no FAQ and no troubleshooting doc exist** (`grep` over `docs/*.md` returns nothing for either) — which is precisely what a stuck user searches for. Continues FR-39 #951, which shipped repo metadata and comparison docs but no site. Design: an in-repo static generator at `ui/docs/` — `markdown-it`+`dompurify` are **already `ui` deps**, and because stage 2 is `COPY ui/ .` and stage 3 copies `ui/dist`, **the Dockerfile is untouched**; output is HTML + one stylesheet + ~5 KB of JS with **no Vue runtime on a docs page** (LCP is a ranking signal). 🔑 The facts that rot are **generated from code**: per-OS install commands expand at build time from `enrollCommands.ts` (zero-import pure module, already vitest-locked), so a flag rename fails a test instead of shipping stale docs; and the build **fails** — not warns — on a broken internal link, because a docs site whose own nav 404s costs more SEO than the site earns. ⚠️ Search is a prebuilt JSON index, **not Pagefind**: Pagefind is WASM and would require adding `wasm-unsafe-eval` to `script-src` — widening a security header to ship a docs feature. ⚠️⚠️ The `location /docs/` block carries **no `add_header` and no `expires`**, because nginx inherits `add_header` only when the current level declares none — and this is live in `files/nginx-pod.conf` today: the existing `\.(js|css|…)$` block declares `add_header Cache-Control` and therefore **already drops all seven server-level security headers for every static asset**. ⚠️ `docs/*.md` is deliberately **never republished** — it names fleet hosts and replays incidents; the site links out to it instead. ⚠️⚠️ **Renumbered FR-58 → FR-59 → FR-60 — three number events in one session**, and the arc is the strongest evidence this table has for its own central rule. At claim time `FR-57` was carried by **two** open issues (#1161, #1163), neither on the ledger, so this took FR-58 rather than manufacture a three-way collision. **FR-58 was then taken anyway** while this work sat on a branch (master absorbed #1170, *newsletter sending*), and **FR-59 was taken during the repair** (#1163, which had itself just renumbered off FR-57). 🔑 **The LEDGER arbitrated and moved THIS claim both times, even though its issue id is LOWER than #1170's** — the lower-issue-id rule applies only when both claims have LANDED ON MASTER, and a branch is invisible to every other session, exactly as a memory file is. Same resolution FR-50 recorded when #1086 took its number mid-flight. ⚠️ **Near-miss worth carrying**: resolving this append-only table's conflict BY LINE INDEX looked correct and then looked like it had deleted master's FR-59 row — it had not (master gained it after the rebase), but the technique could not tell the difference. **Re-append onto the other side's version, never resolve by line number, and diff against master afterwards** — that check is the only thing separating a merge from a silent revert. 🔑 The lesson this adds: **pushing a branch is not claiming — the protocol only protects you once the row is on master**, so on long work land the spec-and-row commit early instead of carrying it with the implementation |
| [FR-61](FR-61-vmtest-matrix.md) | [#1199](https://github.com/gjovanov/roomler-ai/issues/1199) | Throwaway-OS install & verify matrix ("vmtest") — every method, every type, every OS, on demand | proposed — `installer-smoke.yml` covers exactly **2 cells** (perUser MSI on a runner, macOS .pkg; the perMachine wxs is only *compiled*): no Linux `.deb` install, no perMachine/SystemContext, no ARM, no Wayland, no enrollment, no remote desktop, no `roomler-desktop`, anywhere in CI — and every recent install-path defect (FR-49, FR-50, the macOS first-class arc, FR-53) shipped for exactly that reason and was found by a HUMAN doing a field install. Design: throwaway VMs on the fleet hosts (Win11 OVMF+swtpm autounattend · Ubuntu 24.04 GNOME **Wayland** cloud-init · arm64 Ubuntu under QEMU TCG · macOS via tart on the MacBook, opt-in), each cell installing from the REAL served scripts/release assets (`install.sh`/`install.ps1` with `--no-enroll`, silent MSI ×3 types incl. `ENABLE_SYSTEM_CONTEXT=1` — the only public property, `.deb`, `.pkg` ± daemon marker, plus ONE Windows wizard walk over WebView2 CDP), enrolling **ephemeral** (FR-51 keys, vmtest org, rows self-clean; the wizard smoke needs a standard single-use token — wrong JWT audience otherwise — and deletes its row), then asserting: agent online → `peers`/`ping` ↔ a permanent anchor → Playwright RD (`framesDecoded>0` AND `currentTime` advances, exact-name agent selection so parallel VMs cannot cross-match) → `roomler-desktop` all 5 views (Windows = CDP DOM asserts; Linux = `--view` second-launch + `virsh screendump` — WebKitGTK has no CDP) → teardown to baseline (graceful shutdown self-unenrolls; reaper is the backstop). RD runs promptless by DESIGN, not by weakening: controller = enrolling admin ⇒ `owner_consent_mode()` Auto (`ws/remote_control.rs:1708`) + `auto_grant_session` default-true at enroll. Repo split: `k8s-cluster-multi` (opt-in playbook 15 + `vmtest-host` role: OVMF/swtpm/qemu-aarch64, `vmtest-net` WITH its `-i virbrX -j ACCEPT` HOST_FW_INPUT rule — the documented DHCP-hang foot-gun, image cache, capacity audit; k8s-VM shrink runbook exists but executes only on measured shortfall — sequential one-VM-per-host ≤8 GB scheduling should avoid it; jupiter = prod storage, windows only) · `roomler-ai-deploy` (`vmtest/vmtest.sh` bake\|run\|destroy\|report, e2e-nightly-shaped report + expected-failures diff + isolated re-run + gh issue on regression) · this repo (the name-filtered RD spec). ⚠️ aarch64 has a `.deb` but NO `roomler-desktop` asset ⇒ ARM desktop cell = N/A asserted as a graceful skip; macOS is aarch64-only; `machine-attended` is a Windows-only concept — the matrix records impossible cells as N/A with the reason, never as silent skips. Ends with a repeatable `vmtest` skill (P8) written only after the run-and-tweak phase (P7) proves every supported cell green |
| [FR-62](FR-62-encoder-rate-changes-without-an-idr.md) | [#1242](https://github.com/gjovanov/roomler-ai/issues/1242) | Encoder rate changes without an IDR | A0 measured on BOTH backends the fleet runs: NVENC PASSES (20 rate-caused IDRs before the A2 patch, 0 after; apply 0.004 ms) and **QSV FAILS** (`MFXVideoENCODE_Reset` refuses in both `low_power` modes; the rebuild it would replace costs 0.34-2.0 s), so `encoder_inplace_rate` stays OFF and 🔑 **A4 is per-backend, not a flat delete — `coarsen_bitrate` is LOAD-BEARING for QSV**, which has no cheap apply path at all. A1+A2+the const flip shipped in 0.4.51. Origin: the first of three FRs replacing the remote-desktop rate-control patch-work (plan `immutable-doodling-neumann`). **34** controllers/heuristics move rate/fps/dims in `encode/`; **9** exist only to ration the cost of a rate change, because `set_bitrate` (`encode/ffmpeg/encoder.rs:1585`) costs an IDR on every move: NVENC via FFmpeg's `reconfig_encoder` (n8.1.2 sets `resetEncoder=1; forceIDR=1` on a bitrate-only change), QSV/AMF/VT via a full rebuild under a comment that predates FFmpeg 6.0's per-frame `update_bitrate()`. 🔑 our QSV runs **CBR not QVBR** (`select_rc_mode` tests `rc_max_rate == bit_rate` first) — why every 0.4.49 frame was exactly `target ÷ fps ÷ 8` bytes. A0 measures the IDR cost on real silicon (`encoder-smoke --reconfigure-sweep`), A1 gives QSV/NVENC the in-place path behind `encoder_inplace_rate`, A2 carries FFmpeg patches (`0001-nvenc-no-idr`, `0002-videotoolbox-runtime-bitrate`) through the three vendor builders with a `ROOMLER-PATCHES.txt` hash gate, A4 deletes the nine heuristics. Precedes FR-63/64 |
| [FR-63](FR-63-one-delay-based-rate-controller.md) | [#1243](https://github.com/gjovanov/roomler-ai/issues/1243) | One delay-based rate controller for remote desktop | B-opener + **B0 shipped**. The B0 simulator (`encode::sim`, test-only, 11 tests on the default build) drives the SHIPPED `SlowStart` and `AimdController` against a deterministic token-bucket link, which answered the AC0b question the FIELD could not: no reachable host produces a pipe thin enough for the baseline arm to fail, and three cells were tried. Replaying the recorded 213 kbps corp-VPN cell, arm A commits 2.55 M and peaks at a 10 016 ms paint; arm B commits 1.5 M and peaks at 1 226 ms (over-drive 19.6 M → 4.1 M bits, settling 17 → 7 windows). ⚠️ That is evidence about the LAW, not the fleet — AC0b stays open until a real thin path runs it. 🔑 Two findings the field would not have shown: most of arm A's harm is the opening KEYFRAME (a 265 KB IDR is 10 s of that pipe, and with nothing arriving the P1 floor relief has no measurement so the floor stays pinned), and the ramp protects roughly ONE window on a pipe thinner than `OPEN_BPS` because `on_congestion` ends it permanently and the flat floor re-pins. 🔑 Two of the three bugs found building it were in the HARNESS — backpressure that never reached the law made the AIMD look broken. Origin: replaces **8** estimators of one quantity (occupancy AIMD, blocked-send goodput, FR-35 learner + memory, FR-59 P1/P3/P4/P6/P8, FR-15 age loop) composed with "the lower of" rules in `governor.rs:482/708` with ONE pure GCC-shaped controller (`encode::ratectl`), **shadow-first**. 🔑 the controller holds on the age LEVEL not the growth derivative (the lesson FR-59 paid for twice); `RateDomain { floor, ceiling }` is the single source of pins so a second 1.5 M pin cannot exist; the remembered rate and `PathClass` are priors, not loops. B0 = a deterministic `PipeSim`/`ViewerSim` + CSV trace-replay of recorded field series (4 fixtures) — the acceptance gate the `tc netem` harness structurally could not be. B1 runs beside the governor logging `shadow_*` in the heartbeat for a release; flip on ≥90 % agreement in elevated-age windows across ≥40 sessions; B3 deletes the eight and their switches. Depends on FR-62 (a controller with a dead-band assumes rate moves are free) |
| [FR-64](FR-64-remote-desktop-never-rides-the-overlay.md) | [#1244](https://github.com/gjovanov/roomler-ai/issues/1244) | Remote desktop never rides the overlay | proposed — a corp laptop's RC session nominated the OVERLAY host pair (`100.65.0.5 ↔ 100.65.0.6`) so the video rode WebRTC→TUN→WireGuard→DERP/TLS→corp VPN, shed by our own DERP mux (100 ms queue-age), 20–400 kbps, seconds of age — while the host's native TURN pair would have carried it. 🔑🔑 the rc.319 interface filter is a **no-op on Windows** (webrtc-util reports every adapter with name `""`) and misses macOS `utunN`; srflx and remote candidates are never filtered; host↔host type-preference wins whenever the overlay pair connects. Decision (operator, after the trade-off analysis): **never for RC** — WebRTC is already E2E, the WG layer buys RC nothing but a hostile floor. C1 = own-adapter address `ip_filter` at gather (never a CIDR, so an ISP CGNAT address is kept) + the srflx `webrtc-ice.patch` + dropping remote overlay candidates; PathClass in the FR-35 memory key. C2 = **#1237**: the two org runtimes evict each other's `/96` route every guard wave (~100 forced-rekeys/min on every peer; and the primary's block floor is silently withheld) — an own-adapter registry in tunnel-core exempts siblings. SSH/tunnels keep the overlay |
| [FR-65](FR-65-blocking-work-on-async-runtimes.md) | [#1255](https://github.com/gjovanov/roomler-ai/issues/1255) | Blocking work on async runtimes — measure first, then remove | **P0 shipped and field-read on 0.4.60** — the watch (0.4.55, 100 ms bar #1259), `open_ms`/`other_ms` (#1279) and the `work_us` decision + off-thread encoder open (#1284) are live on CORPLAP-1, where the first-pass breakdown now sums to its total on two builds (`open_ms` 748 → 789 ms across #1284: unchanged, as #1288 predicted) and `hevc_qsv` survives a `spawn_blocking` open. The untimed 0.5–1 s of 0.4.55–0.4.58 was the encoder OPEN. Still open: the idle-capture "storm gone" half of #1284 (needs an attended-desktop read), AC1/AC2, and P1's real goal, make-before-break. Plan re-aimed (#1288): the media pipeline leaves the runtime, not per-site wraps. Origin — FR-62 measured a QSV encoder open at **340–390 ms above 1.5 Mbps and 1.3–2.0 s at ≤1 Mbps** (both `low_power` modes) running **inline on the pump thread** for constrained sessions — the exact band where those targets live; #1254 fixed the RATE path only. 🔑 **Why it hid for months is the thesis of this FR**: the pump already measures `avg_capture_ms`/`avg_scale_ms`/`avg_encode_ms`/`avg_send_ms` and the 2 s stall appeared in **none** of them — the apply/rebuild phase was untimed, and a per-heartbeat AVERAGE cannot represent a single outlier anyway. It surfaced only because `set_ms` was instrumented for A0, for an unrelated reason. ⚠️ `roomlerd` is ONE process — overlay, DERP, tunnels, WS control plane and RC share runtimes, so a long synchronous call taxes all of them. P0 = a threshold-gated per-iteration stall watch (two `Instant::now()`, phase breakdown reusing the existing timers **plus the missing `apply_ms`**, **max** beside avg, one rate-limited WARN >250 ms; deliberately NOT `tracing` spans, whose per-phase-per-frame cost is the profile this FR removes) and it BLOCKS every other phase. P1 = dims/chroma/backend rebuilds off-thread, the twin of #1254 and the largest known win: it fires exactly when the session is already struggling, and the swap machinery **structurally cannot carry it** — `rebuild_spec(&self, bps: u32)` carries only a bitrate and `adopt_rebuilt` DISCARDS a geometry change, so a dims change runs inline *and* throws away any in-flight rate swap. P2 = the per-frame encode call, **confirmed mechanism / unquantified harm**: `encode()` is `async fn` in signature only (body is `self.encode_sync(&frame)`, zero `.await`, and `spawn_blocking` appears nowhere in `encode/`), so ~13 ms/frame runs on a shared tokio worker — measured with a **runtime canary** (a 10 ms task recording its own lateness: the SYMPTOM, not a proxy) before any fix, and a dedicated encoder thread rather than per-frame `spawn_blocking` if starvation is shown. P3 overlay guard cadence (debounce, **never disable** — the guard exists so a hostile VPN cannot self-wedge the host), P4 the caps-probe respawning every ~10 s for ~3 h despite its `OnceLock`, P5 the rest triaged by what the canary says hurts. ⚠️ Explicitly NOT in scope: the DataChannel send path (`avg_send_ms` **0.013 ms** — not a bottleneck, must not be "optimised") and the SCTP stalls (a FORWARD-TSN parser bug already fixed, not a blocking-design problem). 🔑 AC7 is the rule the arc turns on: **every fix carries a before/after from the same instrument — a fix with no measurement is not shipped** |
| [FR-66](FR-66-config-probe-false-re-enrollment-error.md) | [#1263](https://github.com/gjovanov/roomler-ai/issues/1263) | A healthy host is told to re-enroll, on every single service start | **P1+P2 shipped, mutation-checked; P3 field-verify pending a release.** A feature-flag probe (`netd_enabled()`, `win_service/supervisor.rs:958`) reads the MACHINE-GLOBAL config for one optional boolean; its behaviour is right (`.ok()` ⇒ absent means off) but it goes through `config::load`, which logs `the host must be re-enrolled` at ERROR on the both-copies-missing arm. On a user-context install (worker `session_id=1 elevated=true`, config in `%APPDATA%`) that path is legitimately absent, so a fully healthy host — both enrollments connected, overlay carrying traffic, config rewritten that morning — is told to re-enroll on EVERY service start. ⚠️ The prescribed remedy is DESTRUCTIVE: removal is final and a re-enrolled device never gets its old overlay address back. ⚠️ It also destroys the signal it exists to send — the genuine all-NUL case emits the identical line, so the message designed to be unmissable is now start-up noise. ⚠️ Invisible to every health check: `status`/`peers`/`is_online` all read healthy, the inverse of the `systemctl is-active` trap. 🔑 The shape: **a shared helper logging at the severity of its WORST caller** — do NOT fix it by softening `load`'s ERROR, that trades a false alarm for a missed one. Fix = `config::read_if_present` (probe reader, debug-level). ⚠️ The P2 audit of all 37 call sites found a SECOND instance, arguably worse: the ENROLL path reused an existing `machine_id` via `load(..).ok()`, so enrolling a clean machine — which by definition has no config — logged `the host must be re-enrolled` mid-enrollment |
| [FR-67](FR-67-update-outcome-observability.md) | [#1267](https://github.com/gjovanov/roomler-ai/issues/1267) | An update's outcome is written, and nothing ever reads it | proposed — found field-testing 0.4.48: `last-install.json` sits at `InProgress` on every Linux host (one still showed `agent-v0.4.16` from 2026-08-29 while running 0.4.50). 🔑 **Two defects, and the cgroup is the lesser one**: `install_tarball_linux` completes synchronously and returns `std::process::id()` — the DAEMON's own pid (`updater.rs:1577`) — which `watch()` then waits on (`post_install.rs:221`), so the watcher's only exit condition is its parent's death while that same death is what kills it via `KillMode=control-group`. Removing the wait needs no systemd at all. ⚠️ A transient-unit design was **refuted before implementation**: `systemd-run` exits 0 once the start job is enqueued, so an exec failure is silent under `--collect` unless `--service-type=exec` is set; its config kill switch is `restart_required` and therefore inert against the wedge it guards; and it would *create* a PID-reuse false-`Timeout` mode. ⚠️ `post_install::read_outcome()` has **zero callers**, so even Windows — where a verdict IS computed — surfaces nothing |
| [FR-68](FR-68-stress-cells-multi-org-and-ipam.md) | [#1272](https://github.com/gjovanov/roomler-ai/issues/1272) | Stress cells for multi-org guard contention and IPAM growth | proposed — two overlay properties are promises rather than properties. FR-47's growth half ships **dark** (`multi_block_enabled=false`) because its P5d wire-compat claim was never tested — the spec's own words are "must be tested against a pinned old-agent decoder, not argued"; and #1237 (two per-org runtimes evicting each other's `fd72:6f6f:6d6c::/96`, **718 evictions/day on neo16**) reached the field because FR-61's matrix is **single-tenant by construction** — no multi-org cell, no IPAM cell. Three layers, cheapest first: in-repo tests (decoder pin against `agent-v0.4.42`, carve-racing-a-grow, renumber-after-grow `seq`, the one-way door), two VM cells (multi-org **must be win11** — `defend_self_route` is Windows-only; IPAM **ubuntu** — the degradation is NAT *scope*), and a **read-only** fleet arm. 🔑 The pre-P5d damage is narrower than assumed: an old agent gets its OWN block in the per-recipient `cidr`, so only a pre-P5d **subnet router** breaks (it masquerades one block, so block-1→block-0-router traffic black-holes one-way, silently). ⚠️ Closes two gaps in #1246's shipped fix: `foreign_in_block_fp` still uses a raw LUID compare (three of four call sites were converted, the commit message says four), and FR-64 C2's overlap WARN was never implemented — nested blocks still yield nested v6 prefixes. ⚠️ Needs three counters first: route evictions and carrier revalidations have **no counter at all**, only a WARN throttled 1/min/prefix, so a naive `grep -c` under-reports by up to 40× |
