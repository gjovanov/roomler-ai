# Third-party notices

Roomler incorporates third-party software. This file records the components
whose licences place an obligation on us, and how we discharge it. It is not a
complete dependency inventory — for that, run `cargo deny check licenses` (policy
in [deny.toml](deny.toml)) or `cargo license`.

---

## FFmpeg — LGPL-2.1-or-later

The Roomler agent uses FFmpeg for hardware video encoding (NVENC, Intel QSV,
AMD AMF). We build it ourselves from unmodified upstream source with a reduced
component set; **we apply no patches to FFmpeg**, and we do **not** build with
`--enable-gpl` or `--enable-nonfree`. No GPL-only component (x264, x265, …) is
present — CI fails the build if one appears.

### Linkage, and what follows from it

| Platform | Linkage | Obligation |
|---|---|---|
| **Windows** (`roomlerd.exe`) | **static** (`x64-windows-static-md`) | LGPL-2.1 **§6** — recipients must be able to relink |
| **Linux** (`.deb`) | shared (`--enable-shared --disable-static`) | §6(b) satisfied by the shared-library mechanism |
| **macOS** | FFmpeg is not shipped | none |

### Written offer

> **G ROX EOOD** hereby offers, to any third party who has received a copy of a
> Roomler binary that statically links FFmpeg, and **for a period of three (3)
> years from the date that binary was distributed**, to provide:
>
> 1. the complete corresponding machine-readable source code of the FFmpeg
>    libraries as used in that binary, together with the exact scripts and
>    configuration used to build them; and
> 2. the linkable object files of the Roomler component that links FFmpeg,
>    sufficient to relink it against a modified version of the library,
>
> at no charge beyond the cost of physically performing the distribution.
>
> To exercise this offer, write to **legal@roomler.ai** quoting the version
> string reported by `roomlerd --version`.

Item **(1)** is published in advance and needs no request:

**<https://github.com/gjovanov/roomler-ai/releases/tag/vendored-ffmpeg-8.1.2>**
— asset `ffmpeg-<version>-corresponding-source.tar.xz`, containing the pristine
upstream tarball plus the complete build recipe, with a SHA-256 sidecar.
Regenerated by [`.github/workflows/lgpl-source-offer.yml`](.github/workflows/lgpl-source-offer.yml).

Item **(2)** — the relink object files for the statically-linked Windows build —
is served on request today, because the archive is per-release and large.

> ⚠️ **Known gap, recorded rather than glossed.** Serving (2) on request is
> permitted by §6(c) but depends on a human answering mail. The structurally
> better fixes, in preference order, are: switch the Windows build to shared
> FFmpeg (§6(b), removing the obligation entirely, at a measured cost in
> installer size — the MSI is currently 13.13 MB), or publish the object
> archive automatically with each release. Tracked in FR-24 P4b.

FFmpeg's own licence text ships in the corresponding-source bundle. Upstream:
<https://ffmpeg.org/legal.html>

---

## OpenH264 — BSD-2-Clause (source-compiled)

The software H.264 encoder path builds OpenH264 **from source** rather than
downloading Cisco's prebuilt binary.

⚠️ Cisco's patent-royalty grant attaches to *Cisco's own binary module*, not to
the source. Building it ourselves means that grant **does not apply to our
builds**. The code licence (BSD-2-Clause) is unaffected and imposes no
obligation beyond attribution; the open question is AVC/H.264 patent-pool
exposure on commercial distribution, which is a business matter and not
something this file can resolve. Recorded so it is a decision rather than an
oversight.

---

## HEVC / H.265

The agent can encode HEVC via `hevc_nvenc`, `hevc_qsv` and `hevc_amf` — wrappers
around the encoder block in the GPU vendor's own driver, which the vendor
licenses. This is a materially narrower exposure than distributing a software
HEVC encoder, but patent-pool licensing for HEVC distribution (Access Advance,
MPEG LA/Via, Velos) has **not** been reviewed. Recorded as a known, unpriced
business risk.

---

## Vendored upstream forks

`crates/vendored/` holds five upstream crates carrying local patches. Each keeps
its **upstream licence, unmodified**, and is excluded from our SPDX header sweep
(`scripts/licence-classes.sh`). The patch rationale for each is in the root
`Cargo.toml`.

| Crate | Upstream | Licence |
|---|---|---|
| `rtp` | webrtc-rs | MIT/Apache-2.0 |
| `webrtc` | webrtc-rs | MIT/Apache-2.0 |
| `webrtc-ice` | webrtc-rs | MIT/Apache-2.0 |
| `wintun-bindings` | upstream | MIT |
| `scrap` | upstream | MIT |

---

## Other notable components

| Component | Licence | Note |
|---|---|---|
| WireGuard / boringtun | BSD-3-Clause | overlay data plane |
| mediasoup | ISC | conferencing SFU |
| coturn | BSD-3-Clause | TURN relay, deployed not linked |
| quinn | MIT/Apache-2.0 | QUIC |
| smoltcp | 0BSD | userspace netstack |
| russh | Apache-2.0 | Roomler SSH server |
| Wintun | third-party binary | redistributed unmodified, original signature preserved |
| Microsoft VC++ runtime | Microsoft redistributable | shipped per redistribution terms, original signatures preserved |

---

Questions about any notice on this page: **legal@roomler.ai**

G ROX EOOD · Plovdivska 110, 4400 Pazardzhik, Bulgaria · UIC 205174895
