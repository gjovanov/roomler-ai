# Substrings matched (grep -F) against failing-spec lines from the nightly
# e2e sweep. A failing spec containing NONE of these lines is a REGRESSION.
# Keep this list justified — every entry is a failure we UNDERSTAND:
#
# 2026-08-30 — the conference entries are GONE, and the reason they were here
# was wrong twice over. They did not need "mediasoup RTC UDP ports the
# port-forward can't carry": the build host has no route to the pod network at
# all, so no port would have helped — and underneath THAT, a blank `turn.url`
# was advertising an ICE server with an empty URL, which made every browser
# refuse the peer connection outright (#940). Nobody could join a call on any
# stack configured that way. With the browser moved into the app pod (FR-37)
# and #940 shipped, the whole family passes: conference 4/4, conference-multi
# 5/5, conference-chat 2/2, conference-list green — 19/19.
# ⚠️ An entry here is a claim you understand the failure. These three were a
# claim nobody had re-tested since July, and they hid a product bug.
# rc-vp9-444 — ⚠️ the reason recorded here was WRONG (it said "needs an agent
# built with the vp9-444 feature lane attached to the stack"). The spec's own
# header says the harness is self-contained and needs NO agent. Measured
# 2026-09-01, two stacked defects, neither of them an agent:
#   1. the harness ran its code from an INLINE <script>, which the app's own
#      CSP (`script-src 'self'`) forbids — so it never initialised at all
#      against a built image. Fixed: the script is an external sibling file.
#   2. it loads the worker from the Vite DEV path
#      `/src/workers/rc-vp9-444-worker.ts`. A built image answers that with the
#      SPA fallback — index.html, HTTP **200**, text/html — so `new Worker`
#      would parse HTML as a module. The harness now says exactly that instead
#      of dying obscurely; the real built worker is a hashed asset.
# So these two specs have never run in ANY lane, only against `bun run dev`.
# Making them run means building the harness as a Vite entry so the worker URL
# is real and hashed — a production-build change, deliberately not smuggled in
# here. Until then the entry is honest about why.
e2e/rc-vp9-444.spec.ts
# Containerized Chromium intercepts accounts.google.com navigations before
# Playwright routing sees them (the identical GitHub-redirect test passes;
# the server's 307 Location was verified correct directly). 2026-07-28.
clicking Google OAuth button redirects to Google
# (Both specs quarantined 2026-07-29 under #246 are now FIXED and removed:
#  conference-list:76 — stale post-S4 selector, re-scoped to the dashboard
#  card (#248); mention:66 — v-if→v-show stable tippy anchor in MentionList
#  (#250, v20260729-135328). Kept here as a marker in case either regresses.)
