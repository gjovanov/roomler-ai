# FR acceptance-criteria debt — regenerate with scripts/fr-verification-debt.sh --update-baseline
#
# Every line is a CLOSED FR whose spec still carries unticked acceptance
# criteria. An FR closes on field verification, so each of these is either a
# record that was never updated or a verification that never happened, and
# the entry must say WHICH. Counts are pinned exactly, both directions.
#
# ⚠️ Adding a line here is not how an FR closes. It is how an ALREADY-closed
#    FR's debt is made attributable instead of invisible.
#
# ── how this file was first populated, and what its reasons are worth ────────
#
# Seeded 2026-09-23 from the audit that introduced the guard. The split below
# is ONE measurement: whether the issue carries a `## Result` / "field-verified"
# comment at all.
#
#   ⚠️ That is evidence ABOUT THE ISSUE, not about these criteria. A Result
#      comment proves something was verified; it does not prove THIS unticked
#      box was. Absence is the stronger signal of the two — an FR closed with
#      no recorded field evidence anywhere is the case the workflow forbids.
#
# So every reason below is PROVISIONAL and names its evidence rather than
# claiming a verdict. Each needs the FR's owner to either tick the criteria
# against linked evidence (and drop the line) or restate the reason. The guard
# does not care which — it cares that the count stops moving unobserved.
#
# Measured trend, which is why the guard exists at all:
#   2026-09-01   8 closed FRs /28 criteria
#   2026-09-23  19 closed FRs /74 criteria   <- unobserved for three weeks

# ── A. closed with NO recorded field evidence on the issue ───────────────────
# The sharp five. Each criterion here was written down because someone judged
# it necessary, and then the issue closed without it and without a Result.
FR-2=1    # A.1 #770 no Result comment. The one criterion is the PROD field check (a stale pin => 409); never recorded.
FR-10=1   # A.2 #783 no Result comment. The open criterion is the `relay_idr_thrift=0` ROLLBACK path — a kill switch never exercised.
FR-11=8   # A.3 #784 no Result comment, and NOT ONE of its 8 criteria is ticked. The ledger calls it shipped + field-verified on prod 2026-08-27; nothing in the record supports that.
FR-13=6   # A.4 #789 no Result comment. Includes "Field: verified on a real Windows-viewer -> macOS-host session" — a cross-OS keyboard path, exactly the class that regresses silently.
FR-37=1   # A.5 #928 no Result comment. One nightly run settles it: does the nightly write its `LATEST` line from the new path.

# ── B. deliberately not met, and the spec says so ────────────────────────────
FR-29=1   # B.1 #864 the spec DECLARES this criterion "NOT met — and not addressable by P1" (fps under sustained motion). Honest and intentional. Owed: a child FR or an in-spec waiver, so a later reader does not have to infer it.

# ── C. closed WITH field evidence on the issue — what the evidence did NOT reach ─
# 2026-09-25: every one of this group's 55 criteria was matched against its own
# evidence, one at a time — the issue's Result comments, the spec's field log,
# the code, and live reads of prod, the registry and this box. 42 were ticked,
# each with a link. What remains is the 13 the evidence did not reach, and each
# spec now says why beside the box. Six FRs cleared entirely (FR-34, FR-42,
# FR-49, FR-50, FR-53, FR-73). ⚠️ FR-73 was never the docs-rule breach this file
# first called it: its docs had existed since #1401; only the tick was missing.
FR-12=4   # C.2  #788 the four P1 behaviours P1 LIVE handed to a human and nobody recorded: first-login auto-open once, the `?`/menu entry points, empty states, deep-link click-through. One browser session with a fresh account.
FR-32=4   # C.3  #898 the thread ticked all seven; four do not survive the code. ⚠️ The compile-time classification of PlanLimits fields NEVER existed (the match covers Limit's variants, not the struct's fields; two fields unclassified since the start) — a false claim in `quota.rs` too. Also: one deliberate hand-rolled read, members' Warn/Off not route-proven, no per-limit tests.
FR-39=1   # C.5  #951 the demo criterion names enroll -> ssh -> forward; the embedded demo shows desktop + mesh ping. Same substitution as FR-41; resolves with it.
FR-41=2   # C.6  #965 the embedded 09-01 re-shoot swapped on-camera enrolment and ssh + forward for a mesh ping; the operator accepted the take's cosmetics, not the substitution, and the spec never recorded it. Re-shoot or amend with the operator.
FR-54=1   # C.11 #1130 wording, not behaviour: by design the network ROW survives an apply, so a second run lists it with no nodes rather than "reports nothing". Amend the criterion to match the design and tick.
FR-57=1   # C.12 #1161 the image RAN on a native aarch64 runner but no arm64 host ever PULLED it; the Result declined to claim it. One `docker pull` on an arm64 host.

# ── nodocs: bound by the docs-before-close rule, and no docs criterion ───────
#
# The rule (PR #1401, merged 2026-09-05T20:49:42Z) makes a docs criterion
# mandatory in every spec. An FR is bound by it if its issue opened, or it
# closed, after that moment. These are bound and carry no such criterion, so
# the debt pins above cannot see their docs at all — there is no box to be
# unticked. Each line is removed the day its spec gains the criterion.
#
# Four were found when the check was written; none remain. FR-74 and FR-81,
# both OPEN, were given the criterion in #1598, so the debt pins can see their
# docs. FR-7 and FR-66, both CLOSED, could not be fixed that way — an unticked
# criterion on a closed FR only moves it to the debt list, and a ticked one
# without the docs would be a false record — so their docs were WRITTEN first
# (2026-09-25: docs/code-signing.md, docs/installation.md) and each criterion
# added ticked in the same PR. Writing FR-7's also paid its one debt pin: the
# Apple criterion had been field-verified on 2026-09-08 and never ticked.
#
# A line appearing here means an FR bound by the rule was written, or closed,
# without the criterion — which is what happened next:
#
# ⚠️ FR-69 is deliberately ABSENT. It closed at 11:53Z on 2026-09-05 with no
#    reader-facing docs — the close the operator made the rule ABOUT — and its
#    owed docs landed in #1401 itself. It is the reason the boundary is a
#    timestamp and not a date.
nodocs FR-22  # opened before the rule, CLOSED 2026-09-25T13:20Z — so bound on close — by the merge of the autopilot's ap-819 work (d5db02c0f touched only the spec). No docs criterion, no reader-facing doc of TTFF anywhere; the guard failed on master 2 s later and stayed red. Pinned so master is green again; the docs are owed (the phase-aware `requesting` bound, #821, and where the stall was localized). The FR-81 shape again: a card looks done when the docs box was never written.
