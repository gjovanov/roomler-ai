[package]
name = "roomler-ai-config"
license = "AGPL-3.0-only"
publish = false
version.workspace = true
edition.workspace = true

[dependencies]
config.workspace = true
serde.workspace = true
serde_json.workspace = true

[dev-dependencies]
# FR-20 P5 — parses the shipped `config/relay-costs.toml` in a test, so a
# mistyped key there cannot fail silently into an unpriced deployment.
toml.workspace = true
