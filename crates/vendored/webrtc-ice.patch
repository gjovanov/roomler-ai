diff -ruN --strip-trailing-cr upstream/Cargo.toml vendored/Cargo.toml
--- upstream/Cargo.toml	1970-01-01 01:00:01.000000000 +0100
+++ vendored/Cargo.toml	2026-07-26 23:32:09.464185600 +0200
@@ -1,13 +1,14 @@
-# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
-#
-# When uploading crates to the registry Cargo will automatically
-# "normalize" Cargo.toml files for maximal compatibility
-# with all versions of Cargo and also rewrite `path` dependencies
-# to registry (e.g., crates.io) dependencies.
-#
-# If you are reading this file be aware that the original Cargo.toml
-# will likely look very different (and much more reasonable).
-# See Cargo.toml.orig for the original contents.
+# Vendored copy of webrtc-ice 0.12.0 from crates.io.
+# Upstream is verbatim EXCEPT (P5 — the delta IS
+# `crates/vendored/webrtc-ice.patch`; re-derive with
+# `scripts/revendor-webrtc-ice.sh`, drift-checked in CI):
+#   - src/agent/agent_gather.rs: patched gather_candidates_relay to add
+#     a TURNS-over-TLS-over-TCP branch (was a NOT_PLANNED TODO upstream),
+#     using the workspace `tcp-turn-conn` crate's Conn adapter.
+#   - src/agent/agent_selector.rs: per-attempt STUN build failures
+#     downgraded log::error!→debug! with greppable targets (2026-05-18
+#     field log-flood fix; not session-fatal — ICE keeps trying pairs).
+#   - this Cargo.toml: the `tcp-turn-conn` path dependency below.
 
 [package]
 edition = "2021"
@@ -89,6 +90,12 @@
 [dependencies.turn]
 version = "0.9.0"
 
+# VENDOR PATCH (P5): the TURNS-over-TLS-over-TCP Conn adapter the patched
+# gather arm uses. Path dep into the workspace — legal for a
+# [patch.crates-io]-resolved, workspace-excluded crate.
+[dependencies.tcp-turn-conn]
+path = "../../tcp-turn-conn"
+
 [dependencies.url]
 version = "2"
 
diff -ruN --strip-trailing-cr upstream/src/agent/agent_gather.rs vendored/src/agent/agent_gather.rs
--- upstream/src/agent/agent_gather.rs	2006-07-24 03:21:28.000000000 +0200
+++ vendored/src/agent/agent_gather.rs	2026-07-26 23:32:09.451846100 +0200
@@ -631,12 +631,30 @@
                     let server_addr = match net2.resolve_addr(is_ipv4, &host_port).await {
                         Ok(addr) => addr,
                         Err(err) => {
-                            log::warn!(
-                                "[{}]: failed to resolve stun host: {}: {}",
-                                agent_internal2.get_name(),
-                                host_port,
-                                err
-                            );
+                            // A v4-only host resolving the STUN host for the IPv6
+                            // network type gets "No available ipv6 IP address
+                            // found" — benign (the host simply has no IPv6), but
+                            // it fired a WARN per url per gather → thousands of
+                            // noise lines on corp v4-only endpoints (roomler fleet
+                            // diag 2026-07-13: ~1770× on the overlay hosts). Log
+                            // the no-address-of-that-family case at DEBUG; keep a
+                            // genuine DNS failure at WARN.
+                            let es = err.to_string();
+                            if es.contains("No available") && es.contains("address found") {
+                                log::debug!(
+                                    "[{}]: no {} local address; skipping srflx to {}",
+                                    agent_internal2.get_name(),
+                                    network,
+                                    host_port,
+                                );
+                            } else {
+                                log::warn!(
+                                    "[{}]: failed to resolve stun host: {}: {}",
+                                    agent_internal2.get_name(),
+                                    host_port,
+                                    es
+                                );
+                            }
                             return Ok(());
                         }
                     };
@@ -775,8 +793,20 @@
 
                 let turn_server_addr = format!("{}:{}", url.host, url.port);
 
-                let (loc_conn, rel_addr, rel_port) =
-                    if url.proto == ProtoType::Udp && url.scheme == SchemeType::Turn {
+                // VENDOR PATCH (TURNS-over-TLS-over-TCP):
+                // Upstream only implements (Udp, Turn). Corporate firewalls
+                // that block all outbound UDP leave (Tcp, Turns) as the
+                // only viable relay path. See tcp_turn_conn.rs for the
+                // framing notes and upstream-issue context.
+                let (transport_proto, transport_scheme) = (url.proto, url.scheme);
+                let turn_host_for_sni = url.host.clone();
+
+                let (loc_conn, rel_addr, rel_port): (
+                    Arc<dyn util::Conn + Send + Sync>,
+                    String,
+                    u16,
+                ) = match (transport_proto, transport_scheme) {
+                    (ProtoType::Udp, SchemeType::Turn) => {
                         let loc_conn = match net2.bind(SocketAddr::from_str("0.0.0.0:0")?).await {
                             Ok(c) => c,
                             Err(err) => {
@@ -793,19 +823,60 @@
                         let rel_addr = local_addr.ip().to_string();
                         let rel_port = local_addr.port();
                         (loc_conn, rel_addr, rel_port)
-                    /*TODO: case url.proto == ProtoType::UDP && url.scheme == SchemeType::TURNS{
-                    case a.proxyDialer != nil && url.Proto == ProtoTypeTCP && (url.Scheme == SchemeTypeTURN || url.Scheme == SchemeTypeTURNS):
-                    case url.Proto == ProtoTypeTCP && url.Scheme == SchemeTypeTURN:
-                    case url.Proto == ProtoTypeTCP && url.Scheme == SchemeTypeTURNS:*/
-                    } else {
+                    }
+                    (ProtoType::Tcp, SchemeType::Turns) => {
+                        // VENDOR PATCH: TURNS over TLS over TCP relay path.
+                        let tcp = match tokio::net::TcpStream::connect(&turn_server_addr).await {
+                            Ok(s) => s,
+                            Err(err) => {
+                                log::warn!(
+                                    "[{}]: TURNS/TCP connect failed for {}: {}",
+                                    agent_internal2.get_name(),
+                                    turn_server_addr,
+                                    err
+                                );
+                                return Ok(());
+                            }
+                        };
+                        // VENDOR PATCH: the adapter lives in the workspace's
+                        // `tcp-turn-conn` crate (P5 extraction — the fork's
+                        // whole substance in one auditable place).
+                        let adapter = match tcp_turn_conn::TcpTurnConn::connect_tls(
+                            tcp,
+                            &turn_host_for_sni,
+                        )
+                        .await
+                        {
+                            Ok(c) => Arc::new(c) as Arc<dyn util::Conn + Send + Sync>,
+                            Err(err) => {
+                                log::warn!(
+                                    "[{}]: TURNS/TCP TLS handshake failed for {}: {}",
+                                    agent_internal2.get_name(),
+                                    turn_server_addr,
+                                    err
+                                );
+                                return Ok(());
+                            }
+                        };
+                        // RELATED-ADDRESS is informational on relay candidates;
+                        // emit 0.0.0.0:0 rather than leaking our TCP source port.
+                        (adapter, "0.0.0.0".to_string(), 0_u16)
+                    }
+                    _ => {
                         log::warn!(
-                            "[{}]: Unable to handle URL in gather_candidates_relay {}",
+                            "[{}]: Unable to handle URL in gather_candidates_relay {} \
+                             (vendor patch supports only Udp+Turn and Tcp+Turns)",
                             agent_internal2.get_name(),
                             url
                         );
                         return Ok(());
-                    };
+                    }
+                };
 
+                // Snapshot username for the diagnostic log on allocate
+                // failure below — `url.username` is consumed by
+                // `ClientConfig` so we can't reference it later.
+                let username_for_log = url.username.clone();
                 let cfg = turn::client::ClientConfig {
                     stun_serv_addr: String::new(),
                     turn_serv_addr: turn_server_addr.clone(),
@@ -815,7 +886,15 @@
                     software: String::new(),
                     rto_in_ms: 0,
                     conn: loc_conn,
-                    vnet: Some(Arc::clone(&net2)),
+                    // VENDOR PATCH: vnet is the virtual-net abstraction that
+                    // can open additional sockets for keepalives. Over TCP
+                    // every byte MUST go through the same connection, so we
+                    // omit vnet for the TCP branch.
+                    vnet: if transport_proto == ProtoType::Tcp {
+                        None
+                    } else {
+                        Some(Arc::clone(&net2))
+                    },
                 };
                 let client = match turn::client::Client::new(cfg).await {
                     Ok(client) => Arc::new(client),
@@ -844,10 +923,24 @@
                     Ok(conn) => Arc::new(conn),
                     Err(err) => {
                         let _ = client.close().await;
+                        // Roomler vendor diagnostic (2026-05-18 a third field-test host
+                        // / a second field-test host): include the scheme + transport
+                        // so we can correlate which TURN URL flavour
+                        // failed. `err` is typically just "attribute
+                        // not found" — needs investigation downstream of
+                        // this log line via packet capture / coturn
+                        // verbose logs. See `docs/turn-allocate-debug.md`.
                         log::warn!(
-                            "[{}]: Failed to allocate on turn.Client {} {}",
+                            "[{}]: TURN allocate failed addr={} scheme={:?} transport={:?} username={} err={}",
                             agent_internal2.get_name(),
                             turn_server_addr,
+                            transport_scheme,
+                            transport_proto,
+                            // username is the REST-API expiry:user_id
+                            // pair; logging it is fine (it's not a
+                            // secret, and it ties the failure to a
+                            // specific coturn server-side session).
+                            if username_for_log.is_empty() { "<empty>" } else { username_for_log.as_str() },
                             err
                         );
                         return Ok(());
diff -ruN --strip-trailing-cr upstream/src/agent/agent_selector.rs vendored/src/agent/agent_selector.rs
--- upstream/src/agent/agent_selector.rs	2006-07-24 03:21:28.000000000 +0200
+++ vendored/src/agent/agent_selector.rs	2026-07-26 23:32:09.447846600 +0200
@@ -137,7 +137,13 @@
                 };
 
                 if let Err(err) = result {
-                    log::error!("{}", err);
+                    // Roomler vendor patch (2026-05-18 a third field-test host log-flood
+                    // fix): downgrade per-attempt STUN build failures
+                    // to DEBUG so they don't drown the rolling log,
+                    // and prefix with a target so they're greppable.
+                    // Per-candidate-pair build failures are not
+                    // session-fatal — ICE keeps trying other pairs.
+                    log::debug!(target: "webrtc_ice::nominate_pair", "STUN message build failed: {}", err);
                     None
                 } else {
                     log::trace!(
@@ -297,7 +303,10 @@
         };
 
         if let Err(err) = result {
-            log::error!("{}", err);
+            // Roomler vendor patch (2026-05-18 a third field-test host log-flood fix):
+            // see nominate_pair above; per-candidate STUN build
+            // failures are not session-fatal.
+            log::debug!(target: "webrtc_ice::ping_candidate", "STUN message build failed: {}", err);
         } else {
             self.send_binding_request(&msg, local, remote).await;
         }
@@ -449,7 +458,10 @@
         };
 
         if let Err(err) = result {
-            log::error!("{}", err);
+            // Roomler vendor patch (2026-05-18 a third field-test host log-flood fix):
+            // see nominate_pair above; per-candidate STUN build
+            // failures are not session-fatal.
+            log::debug!(target: "webrtc_ice::ping_candidate", "STUN message build failed: {}", err);
         } else {
             self.send_binding_request(&msg, local, remote).await;
         }
