diff -ruN --strip-trailing-cr upstream/.cargo-ok vendored/.cargo-ok
--- upstream/.cargo-ok	1970-01-01 01:00:00.000000000 +0100
+++ vendored/.cargo-ok	2026-09-02 14:51:25.631029900 +0200
@@ -0,0 +1 @@
+{"v":1}
\ No newline at end of file
diff -ruN --strip-trailing-cr upstream/Cargo.toml vendored/Cargo.toml
--- upstream/Cargo.toml	1970-01-01 01:00:01.000000000 +0100
+++ vendored/Cargo.toml	2026-09-02 14:51:25.634072200 +0200
@@ -1,13 +1,17 @@
-# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
-#
-# When uploading crates to the registry Cargo will automatically
-# "normalize" Cargo.toml files for maximal compatibility
-# with all versions of Cargo and also rewrite `path` dependencies
-# to registry (e.g., crates.io) dependencies.
-#
-# If you are reading this file be aware that the original Cargo.toml
-# will likely look very different (and much more reasonable).
-# See Cargo.toml.orig for the original contents.
+# Vendored copy of webrtc-sctp 0.11.0 from crates.io.
+# Upstream is verbatim EXCEPT (the delta IS
+# `crates/vendored/webrtc-sctp.patch`; re-derive with
+# `scripts/revendor-webrtc-sctp.sh`, drift-checked in CI):
+#   - src/chunk/chunk_forward_tsn.rs: `unmarshal` bounds the stream-entry
+#     loop by the CHUNK's declared length instead of the packet buffer.
+#     Upstream counted every byte to the end of the packet as stream
+#     entries, so a FORWARD-TSN with any chunk bundled after it — what
+#     Chrome's dcSCTP sends (FORWARD-TSN + SACK) whenever it abandons a
+#     message on an unreliable DataChannel — failed with `chunk too
+#     short` and the WHOLE packet, the SACK included, was discarded.
+#     Field 2026-09-02: 549 such drops in one day on one host, each a
+#     lost acknowledgement that stalled the outbound video on an RTO.
+#   - src/chunk/chunk_test.rs: the regression tests for that shape.
 
 [package]
 edition = "2021"
diff -ruN --strip-trailing-cr upstream/src/chunk/chunk_forward_tsn.rs vendored/src/chunk/chunk_forward_tsn.rs
--- upstream/src/chunk/chunk_forward_tsn.rs	2006-07-24 03:21:28.000000000 +0200
+++ vendored/src/chunk/chunk_forward_tsn.rs	2026-09-02 14:51:25.659365800 +0200
@@ -67,19 +67,30 @@
         }
 
         let mut offset = CHUNK_HEADER_SIZE + NEW_CUMULATIVE_TSN_LENGTH;
-        if buf.len() < offset {
+        // The end of THIS chunk. `buf` is the remainder of the packet (see
+        // `Packet::unmarshal`), so anything past `chunk_end` belongs to the
+        // next bundled chunk, not to this one. `ChunkHeader::unmarshal` has
+        // already refused a declared length longer than the buffer.
+        let chunk_end = CHUNK_HEADER_SIZE + header.value_length();
+        if buf.len() < offset || chunk_end < offset {
             return Err(Error::ErrChunkTooShort);
         }
 
-        let reader = &mut buf.slice(CHUNK_HEADER_SIZE..CHUNK_HEADER_SIZE + header.value_length());
+        let reader = &mut buf.slice(CHUNK_HEADER_SIZE..chunk_end);
         let new_cumulative_tsn = reader.get_u32();
 
         let mut streams = vec![];
-        let mut remaining = buf.len() - offset;
+        // roomler fork — bound the stream-entry loop by the chunk's own
+        // declared length. Upstream counted `buf.len() - offset`, i.e. every
+        // byte to the end of the PACKET: a FORWARD-TSN with a chunk bundled
+        // after it (dcSCTP sends FORWARD-TSN + SACK together whenever it
+        // abandons a message on an unreliable stream) ran this loop into the
+        // next chunk, the stream slice came up empty, and the whole packet —
+        // the SACK included — was dropped as `chunk too short`. Each such
+        // drop is a lost acknowledgement; the sender then stalls on an RTO.
+        let mut remaining = chunk_end - offset;
         while remaining > 0 {
-            let s = ChunkForwardTsnStream::unmarshal(
-                &buf.slice(offset..CHUNK_HEADER_SIZE + header.value_length()),
-            )?;
+            let s = ChunkForwardTsnStream::unmarshal(&buf.slice(offset..chunk_end))?;
             offset += s.value_length();
             remaining -= s.value_length();
             streams.push(s);
diff -ruN --strip-trailing-cr upstream/src/chunk/chunk_test.rs vendored/src/chunk/chunk_test.rs
--- upstream/src/chunk/chunk_test.rs	2006-07-24 03:21:28.000000000 +0200
+++ vendored/src/chunk/chunk_test.rs	2026-09-02 14:51:25.659365800 +0200
@@ -205,6 +205,61 @@
     Ok(())
 }
 
+/// roomler fork — a FORWARD-TSN followed by a bundled chunk in the SAME
+/// buffer (what `Packet::unmarshal` hands every chunk parser: the remainder
+/// of the packet) must parse ITS stream entries only. Upstream counted to
+/// the end of the buffer, ran into the next chunk, and failed the packet.
+#[test]
+fn test_chunk_forward_tsn_ignores_bundled_trailing_chunk() -> Result<()> {
+    // FORWARD-TSN (len 12: new cumulative TSN 3, one stream 4/5) followed by
+    // a SACK (type 3, len 16) — the shape dcSCTP sends when it abandons a
+    // message on an unreliable stream.
+    let bundled = Bytes::from_static(&[
+        0xc0, 0x0, 0x0, 0xc, 0x0, 0x0, 0x0, 0x3, 0x0, 0x4, 0x0, 0x5, // FORWARD-TSN
+        0x3, 0x0, 0x0, 0x10, 0x0, 0x0, 0x0, 0x9, 0x0, 0x0, 0x4, 0x0, 0x0, 0x0, 0x0, 0x0, // SACK
+    ]);
+    let actual = ChunkForwardTsn::unmarshal(&bundled)?;
+    assert_eq!(actual.new_cumulative_tsn, 3);
+    assert_eq!(actual.streams.len(), 1, "only this chunk's entries");
+    assert_eq!(actual.streams[0].identifier, 4);
+    assert_eq!(actual.streams[0].sequence, 5);
+    assert_eq!(
+        actual.value_length(),
+        8,
+        "the packet loop must advance by the chunk, not by the buffer"
+    );
+    Ok(())
+}
+
+/// roomler fork — and the packet level: both chunks come out.
+#[test]
+fn test_packet_with_forward_tsn_then_another_chunk_parses_both() -> Result<()> {
+    use crate::packet::Packet;
+    let fwd = ChunkForwardTsn {
+        new_cumulative_tsn: 3,
+        streams: vec![ChunkForwardTsnStream {
+            identifier: 4,
+            sequence: 5,
+        }],
+    };
+    let trailing = ChunkForwardTsn {
+        new_cumulative_tsn: 9,
+        streams: vec![],
+    };
+    let packet = Packet {
+        source_port: 5000,
+        destination_port: 5000,
+        verification_tag: 0x1234_5678,
+        chunks: vec![Box::new(fwd), Box::new(trailing)],
+    };
+    let raw = packet.marshal()?;
+    let parsed = Packet::unmarshal(&raw)?;
+    assert_eq!(parsed.chunks.len(), 2, "the chunk after a FORWARD-TSN survives");
+    assert_eq!(parsed.chunks[0].header().typ, CT_FORWARD_TSN);
+    assert_eq!(parsed.chunks[1].header().typ, CT_FORWARD_TSN);
+    Ok(())
+}
+
 #[test]
 fn test_chunk_forward_tsn_unmarshal_failure() -> Result<()> {
     let tests = vec![
