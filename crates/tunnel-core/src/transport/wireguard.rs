//! `wireguard-v1` transport — v0.5 (Option B).
//!
//! Stub in v1 so the capability negotiation can be tested end-to-end
//! before the real impl lands. Will pull in `boringtun` behind a
//! Cargo feature.
