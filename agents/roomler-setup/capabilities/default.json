{
  "identifier": "default",
  "description": "Main-window capabilities for the roomler-setup wizard: core defaults plus the event-listen permission (the single-instance 'installer-already-running' signal) and the shell/dialog plugin commands the SPA uses. Without an explicit event grant, Tauri 2 denies plugin:event|listen — which aborted the wizard's listener wiring and left the Next buttons dead (2026-07-17).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "shell:allow-open",
    "dialog:default"
  ]
}
